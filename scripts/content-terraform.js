// Terraform/HCL base content - generated by Claude
module.exports = {

// ============================================================
// GENERAL REVIEW QUALITY (items 1-8)
// ============================================================

"terraform-hcl.general-review-quality.does-the-pr-clearly-state-the-infrastructure-intent-blast-radius-and-rol": {
  whatItMeans: "The PR description should explain what infrastructure is being changed, how many environments or resources are affected (blast radius), and what the rollback strategy is if something goes wrong.",
  whyItMatters: "Infrastructure changes can take down production. Without a clear description of intent and blast radius, reviewers cannot assess risk accurately. A rollback plan ensures the team knows how to recover quickly.",
  howToVerify: "Read the PR description. Does it state what is changing, which environments are targeted, how many resources are affected, and what the rollback plan is? Is there a link to a plan output?",
  exampleComment: "This PR modifies the production VPC routing tables but does not mention a rollback plan. Could you add a section describing how to revert if the new routes cause connectivity issues?",
  codeExamples: [],
  keyTakeaway: "Every infrastructure PR should answer: what changes, where, how big is the blast radius, and how do we undo it.",
  references: []
},

"terraform-hcl.general-review-quality.is-the-change-scoped-to-a-coherent-unit-of-infra-work-and-not-mixing-unr": {
  whatItMeans: "A single PR should address one logical infrastructure change, not bundle unrelated modifications such as networking changes alongside IAM policy updates.",
  whyItMatters: "Mixed-concern PRs are hard to review, hard to roll back, and increase the blast radius of any single change. If a networking change breaks, you do not want to also revert an unrelated IAM fix.",
  howToVerify: "Scan the file list and plan output. Are all changes related to a single infrastructure concern? Could any subset ship independently without the rest?",
  exampleComment: "This PR updates the RDS instance class and also adds new S3 lifecycle rules. These are independent changes. Could we split them so each can be reviewed and applied separately?",
  codeExamples: [],
  keyTakeaway: "One infrastructure concern per PR. Smaller PRs get better reviews and safer applies.",
  references: []
},

"terraform-hcl.general-review-quality.are-environment-targets-explicitly-called-out-dev-stage-prod-regions-acc": {
  whatItMeans: "The PR should clearly state which environments (dev, staging, production), regions, and cloud accounts will be affected by the change.",
  whyItMatters: "Applying changes to the wrong environment is one of the most common and damaging Terraform mistakes. Explicit environment targeting prevents accidental production deployments.",
  howToVerify: "Check the PR description and the tfvars or workspace configuration being used. Is it obvious which environment this targets? Are plan outputs labeled per environment?",
  exampleComment: "Which environment does this target? The PR description does not mention whether this is for dev or prod, and the backend key does not make it obvious.",
  codeExamples: [],
  keyTakeaway: "Always make the target environment unmistakably clear in every infrastructure PR.",
  references: []
},

"terraform-hcl.general-review-quality.is-there-a-plan-artifact-attached-for-each-target-environment": {
  whatItMeans: "A terraform plan output should be attached to the PR for every environment that will be affected, so reviewers can see exactly what will change.",
  whyItMatters: "Reviewing HCL code alone does not show the actual impact. The plan output reveals resource creates, updates, destroys, and attribute-level changes that may not be obvious from the code diff.",
  howToVerify: "Look for plan output in the PR comments or CI artifacts. Is there one for each target environment? Does it show the expected number and type of changes?",
  exampleComment: "I see code changes targeting both staging and production, but only a staging plan is attached. Could you add the production plan output so we can review the full impact?",
  codeExamples: [],
  keyTakeaway: "No plan, no merge. Plan artifacts are the ground truth for what Terraform will actually do.",
  references: []
},

"terraform-hcl.general-review-quality.are-any-manual-console-steps-removed-or-justified-if-unavoidable": {
  whatItMeans: "All infrastructure changes should be codified in Terraform. If any manual console steps are required, they should be explicitly documented and justified.",
  whyItMatters: "Manual steps introduce drift between code and reality, are not reproducible, and break the infrastructure-as-code contract. They also cannot be reviewed or audited like code changes.",
  howToVerify: "Search the PR description and comments for mentions of manual steps, console actions, or CLI commands to run outside of Terraform. Are any of these necessary, or can they be codified?",
  exampleComment: "The PR mentions manually enabling versioning on the S3 bucket after apply. Can we add the versioning configuration block to the Terraform code instead so it is managed declaratively?",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "# After apply, manually enable versioning in the console\nresource \"aws_s3_bucket\" \"data\" {\n  bucket = \"my-data-bucket\"\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "resource \"aws_s3_bucket\" \"data\" {\n  bucket = \"my-data-bucket\"\n}\n\nresource \"aws_s3_bucket_versioning\" \"data\" {\n  bucket = aws_s3_bucket.data.id\n  versioning_configuration {\n    status = \"Enabled\"\n  }\n}"
    }
  ],
  keyTakeaway: "If it is not in code, it does not exist. Codify everything and justify any exceptions.",
  references: []
},

"terraform-hcl.general-review-quality.are-destructive-actions-called-out-explicitly-in-review-notes": {
  whatItMeans: "Any resource destroys, replacements, or data-loss-risk changes should be explicitly highlighted in the PR description so reviewers pay special attention to them.",
  whyItMatters: "Destructive actions like destroying a database or replacing a load balancer can cause outages and data loss. If they are buried in a large plan, reviewers may miss them.",
  howToVerify: "Check the plan output for destroy or replace actions. Are these called out in the PR description? Does the reviewer know which resources will be destroyed?",
  exampleComment: "The plan shows the RDS instance will be destroyed and recreated. This is a data-loss event. Please call this out prominently in the PR description and confirm backups exist.",
  codeExamples: [],
  keyTakeaway: "Always highlight destroys and replacements in review notes. Surprises in production are never welcome.",
  references: []
},

"terraform-hcl.general-review-quality.are-todo-fixme-comments-tied-to-tracked-work-items": {
  whatItMeans: "TODO and FIXME comments in Terraform code should reference a ticket or issue number so the work is tracked and prioritized.",
  whyItMatters: "Untracked TODOs in infrastructure code become permanent tech debt. Without a ticket, nobody owns the follow-up work and it is forgotten.",
  howToVerify: "Search the diff for TODO, FIXME, HACK, or XXX comments. Do they include a ticket reference?",
  exampleComment: "This TODO about migrating to the new provider does not have a tracking ticket. Could you file one and reference it here?",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "# TODO: migrate to aws_s3_bucket_lifecycle_configuration\nresource \"aws_s3_bucket\" \"logs\" {\n  bucket = \"app-logs\"\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "# TODO(INFRA-4521): migrate to aws_s3_bucket_lifecycle_configuration\n# Target: Q2 2025 provider upgrade sprint\nresource \"aws_s3_bucket\" \"logs\" {\n  bucket = \"app-logs\"\n}"
    }
  ],
  keyTakeaway: "Every TODO in infrastructure code should have an owner via a tracked ticket.",
  references: []
},

"terraform-hcl.general-review-quality.are-commits-organized-so-infra-intent-is-easy-to-review-incrementally": {
  whatItMeans: "Commits should be logically structured so reviewers can follow the infrastructure intent step by step, rather than seeing one massive diff.",
  whyItMatters: "Well-organized commits help reviewers understand the reasoning behind each change, make bisecting issues easier, and allow selective reverts if needed.",
  howToVerify: "Read the commit log. Are messages descriptive of infrastructure intent? Are there WIP commits that should be squashed? Does the sequence tell a logical story?",
  exampleComment: "The commit history has several 'fix' and 'wip' commits. Could you squash these into logical units like 'Add VPC peering configuration' and 'Update security group rules' before merge?",
  codeExamples: [],
  keyTakeaway: "Organize commits by infrastructure intent. Future you will thank present you when debugging.",
  references: []
},

// ============================================================
// TERRAFORM FUNDAMENTALS & REPO (items 9-24)
// ============================================================

"terraform-hcl.terraform-fundamentals-repo.is-the-terraform-version-pinned-with-required-version": {
  whatItMeans: "The terraform block should include a required_version constraint to ensure all team members and CI pipelines use a compatible Terraform version.",
  whyItMatters: "Different Terraform versions can produce different plans and state formats. Without pinning, one developer's apply could use a different version than CI, leading to state corruption or unexpected behavior.",
  howToVerify: "Check for a terraform block with required_version in the root module. Is the constraint specific enough to prevent major version drift but flexible enough for patch updates?",
  exampleComment: "There is no required_version constraint. Please add one to prevent version drift between developers and CI.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "terraform {\n  # No version constraint\n  backend \"s3\" {\n    bucket = \"tf-state\"\n  }\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "terraform {\n  required_version = \">= 1.5.0, < 2.0.0\"\n\n  backend \"s3\" {\n    bucket = \"tf-state\"\n  }\n}"
    }
  ],
  keyTakeaway: "Always pin the Terraform version. It is the single most important reproducibility control.",
  references: ["https://developer.hashicorp.com/terraform/language/settings#specifying-a-required-terraform-version"]
},

"terraform-hcl.terraform-fundamentals-repo.is-the-repository-structure-consistent-root-modules-reusable-modules-env": {
  whatItMeans: "The repository should follow a consistent directory structure separating root modules (entry points), reusable modules, and environment-specific configurations.",
  whyItMatters: "Inconsistent structure makes it hard to find resources, understand what applies where, and onboard new team members. A predictable layout reduces review friction.",
  howToVerify: "Check the directory tree. Are root modules, reusable modules, and environment overlays clearly separated? Does the structure match the team's documented conventions?",
  exampleComment: "This new module is placed at the root level alongside environment configs. Should it go under modules/ to match the existing repository structure?",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "# Flat structure mixing everything\n# .\n# +-- main.tf (production resources)\n# +-- dev.tf (dev resources)\n# +-- vpc-module.tf (reusable module inline)\n# +-- variables.tf"
    },
    {
      label: "Good",
      language: "hcl",
      code: "# Clear separation\n# .\n# +-- environments/\n# |   +-- dev/\n# |   |   +-- main.tf\n# |   |   +-- terraform.tfvars\n# |   +-- prod/\n# |       +-- main.tf\n# |       +-- terraform.tfvars\n# +-- modules/\n# |   +-- vpc/\n# |   |   +-- main.tf\n# |   |   +-- variables.tf\n# |   |   +-- outputs.tf\n# |   +-- rds/\n# +-- versions.tf"
    }
  ],
  keyTakeaway: "A consistent directory structure is a force multiplier for team velocity and review quality.",
  references: ["https://developer.hashicorp.com/terraform/language/modules/develop/structure"]
},

"terraform-hcl.terraform-fundamentals-repo.are-root-modules-minimal-and-orchestration-focused-rather-than-logic-hea": {
  whatItMeans: "Root modules should primarily compose and wire together reusable modules, not contain complex resource logic directly.",
  whyItMatters: "Logic-heavy root modules are hard to test, reuse, and review. Keeping root modules thin makes them easier to understand and allows the real logic to be tested in isolation within modules.",
  howToVerify: "Check root module main.tf files. Are they mostly module calls with variable wiring, or do they contain dozens of inline resources with complex conditionals?",
  exampleComment: "This root module has 15 inline resources with conditional logic. Consider extracting the database and networking resources into reusable modules to keep the root module orchestration-focused.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "# root main.tf with inline logic\nresource \"aws_vpc\" \"main\" {\n  cidr_block = var.env == \"prod\" ? \"10.0.0.0/16\" : \"10.1.0.0/16\"\n}\n\nresource \"aws_subnet\" \"public\" {\n  count      = var.env == \"prod\" ? 3 : 2\n  vpc_id     = aws_vpc.main.id\n  cidr_block = cidrsubnet(aws_vpc.main.cidr_block, 8, count.index)\n}\n\n# ... 50 more resources with similar conditionals"
    },
    {
      label: "Good",
      language: "hcl",
      code: "# root main.tf - orchestration only\nmodule \"networking\" {\n  source = \"../../modules/vpc\"\n\n  environment    = var.environment\n  cidr_block     = var.vpc_cidr\n  az_count       = var.az_count\n}\n\nmodule \"database\" {\n  source = \"../../modules/rds\"\n\n  vpc_id         = module.networking.vpc_id\n  subnet_ids     = module.networking.private_subnet_ids\n  instance_class = var.db_instance_class\n}"
    }
  ],
  keyTakeaway: "Root modules should read like a table of contents, not like a novel.",
  references: []
},

"terraform-hcl.terraform-fundamentals-repo.are-module-boundaries-clear-and-aligned-to-ownership-domains": {
  whatItMeans: "Module boundaries should map to logical ownership domains such as networking, compute, database, or security, so that teams can own and evolve their modules independently.",
  whyItMatters: "Poorly drawn module boundaries lead to tight coupling, where changes to one team's infrastructure break another team's module. Clear boundaries enable parallel development and independent lifecycle management.",
  howToVerify: "Review the module structure. Does each module represent a clear domain? Are there modules that mix unrelated concerns (e.g., VPC and application deployment in one module)?",
  exampleComment: "This module creates both the VPC and the ECS cluster. Since networking and compute are owned by different teams, consider splitting them into separate modules.",
  codeExamples: [],
  keyTakeaway: "Draw module boundaries along ownership lines. If two teams need to change the same module, the boundary is wrong.",
  references: []
},

"terraform-hcl.terraform-fundamentals-repo.is-hcl-formatted-terraform-fmt-with-no-noisy-formatting-drift": {
  whatItMeans: "All HCL files should be formatted with terraform fmt so that formatting is consistent and diffs only show meaningful changes.",
  whyItMatters: "Formatting inconsistencies create noisy diffs that obscure real changes. They also cause unnecessary merge conflicts and make code review harder.",
  howToVerify: "Run terraform fmt -check -recursive on the changed files. Are there any formatting differences? Is terraform fmt enforced in CI?",
  exampleComment: "Several files have formatting drift. Please run terraform fmt before pushing. Consider adding a CI check to catch this automatically.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "resource \"aws_instance\" \"web\" {\n    ami = \"ami-12345678\"\n  instance_type=\"t3.micro\"\n      tags={\n    Name=\"web-server\"\n  }\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "resource \"aws_instance\" \"web\" {\n  ami           = \"ami-12345678\"\n  instance_type = \"t3.micro\"\n\n  tags = {\n    Name = \"web-server\"\n  }\n}"
    }
  ],
  keyTakeaway: "Run terraform fmt. Always. Make CI enforce it so you never have to think about it.",
  references: ["https://developer.hashicorp.com/terraform/cli/commands/fmt"]
},

"terraform-hcl.terraform-fundamentals-repo.does-static-validation-pass-terraform-validate-for-changed-modules": {
  whatItMeans: "Running terraform validate should pass for all changed modules, catching syntax errors, invalid references, and type mismatches before the code reaches review.",
  whyItMatters: "Catching validation errors before review saves everyone time. If the code does not validate, the plan will fail too, making the review cycle longer.",
  howToVerify: "Run terraform validate in each changed module directory. Are there any errors? Is validation enforced in CI pre-review?",
  exampleComment: "terraform validate fails on this module because the variable type does not match the default value. Please fix before requesting review.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "variable \"instance_count\" {\n  type    = number\n  default = \"three\"  # type mismatch: string vs number\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "variable \"instance_count\" {\n  type    = number\n  default = 3\n}"
    }
  ],
  keyTakeaway: "terraform validate is free and fast. Run it before every commit.",
  references: ["https://developer.hashicorp.com/terraform/cli/commands/validate"]
},

"terraform-hcl.terraform-fundamentals-repo.are-files-named-predictably-providers-tf-versions-tf-variables-tf-output": {
  whatItMeans: "Terraform files should follow a conventional naming scheme: providers.tf, versions.tf, variables.tf, outputs.tf, main.tf, and optionally locals.tf and data.tf.",
  whyItMatters: "Predictable file names let reviewers quickly locate provider configs, variable definitions, and outputs without searching. It also reduces merge conflicts when multiple people add to the same files.",
  howToVerify: "Check the file names in the module. Do they follow the team convention? Are providers, versions, variables, and outputs in dedicated files?",
  exampleComment: "The provider configuration is embedded in main.tf alongside resources. Please move it to providers.tf to match our file naming convention.",
  codeExamples: [],
  keyTakeaway: "Consistent file names reduce cognitive load. Reviewers should know exactly where to find providers, variables, and outputs.",
  references: ["https://developer.hashicorp.com/terraform/language/modules/develop/structure"]
},

"terraform-hcl.terraform-fundamentals-repo.are-logical-concerns-grouped-to-reduce-merge-conflict-hotspots": {
  whatItMeans: "Related resources should be grouped in the same file, and unrelated resources should be separated, to minimize merge conflicts when multiple developers work on the same module.",
  whyItMatters: "If all resources live in a single main.tf, every change conflicts with every other change. Logical grouping reduces merge pain and makes the codebase navigable.",
  howToVerify: "Check whether large files contain unrelated resource groups that could be split. Are networking, compute, and storage resources in their own files?",
  exampleComment: "main.tf is 500 lines with VPC, RDS, and Lambda resources mixed together. Consider splitting into networking.tf, database.tf, and compute.tf to reduce merge conflicts.",
  codeExamples: [],
  keyTakeaway: "Group related resources in dedicated files. Your team will thank you during merge season.",
  references: []
},

"terraform-hcl.terraform-fundamentals-repo.is-generated-content-excluded-or-clearly-separated-from-authored-code": {
  whatItMeans: "Auto-generated files like .terraform.lock.hcl or terraform-docs output should be clearly separated from hand-written code, and .terraform/ directories should be gitignored.",
  whyItMatters: "Mixing generated and authored content makes review harder. Reviewers waste time on auto-generated diffs and may miss real changes buried in generated noise.",
  howToVerify: "Check .gitignore for .terraform/ and other generated artifacts. Is auto-generated documentation clearly marked? Are generated provider binaries excluded?",
  exampleComment: "The .terraform/ directory is committed to the repository. Please add it to .gitignore and remove it from version control.",
  codeExamples: [],
  keyTakeaway: "Keep generated content out of the review path. Gitignore .terraform/ and mark generated docs clearly.",
  references: []
},

"terraform-hcl.terraform-fundamentals-repo.are-module-readmes-present-and-updated-for-interface-changes": {
  whatItMeans: "Every reusable module should have a README documenting its purpose, inputs, outputs, and usage examples. READMEs should be updated when the module interface changes.",
  whyItMatters: "Without documentation, consumers of a module must read the source code to understand how to use it. Stale READMEs are worse than no README because they mislead users.",
  howToVerify: "Check if changed modules have a README.md. If the module interface (variables, outputs) changed, is the README updated? Consider using terraform-docs to auto-generate.",
  exampleComment: "This PR adds two new required variables to the networking module but the README still shows the old interface. Please update the usage examples.",
  codeExamples: [],
  keyTakeaway: "Module READMEs are the contract with consumers. Keep them in sync with the code.",
  references: ["https://github.com/terraform-docs/terraform-docs"]
},

"terraform-hcl.terraform-fundamentals-repo.are-examples-provided-for-reusable-modules-where-behavior-is-non-obvious": {
  whatItMeans: "Reusable modules with complex behavior should include example configurations in an examples/ directory showing common usage patterns.",
  whyItMatters: "Examples are the fastest way for consumers to understand how a module works. They also serve as integration test fixtures and living documentation.",
  howToVerify: "Check if the module has an examples/ directory with at least one working configuration. Do the examples cover the most common use cases?",
  exampleComment: "This module has five optional feature flags that interact in non-obvious ways. Could you add an examples/complete/ directory showing a full configuration?",
  codeExamples: [],
  keyTakeaway: "Examples are the best documentation. They show, not tell.",
  references: ["https://developer.hashicorp.com/terraform/language/modules/develop/structure"]
},

"terraform-hcl.terraform-fundamentals-repo.are-module-sources-stable-and-intentional-registry-git-ref-local-path": {
  whatItMeans: "Module source references should be intentional: local paths for in-repo modules, registry references for public modules, or pinned git refs for private modules.",
  whyItMatters: "Unstable or ambiguous module sources can pull unexpected versions. A source pointing to a branch HEAD can change without notice, making builds non-reproducible.",
  howToVerify: "Check module source attributes. Are registry modules versioned? Are git sources pinned to tags or commits? Are local paths relative and correct?",
  exampleComment: "This module source points to a git branch (ref=main). Please pin to a specific tag or commit hash for reproducibility.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "module \"vpc\" {\n  source = \"git::https://github.com/org/modules.git//vpc?ref=main\"\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "module \"vpc\" {\n  source  = \"git::https://github.com/org/modules.git//vpc?ref=v2.3.1\"\n}\n\n# Or from registry:\nmodule \"vpc\" {\n  source  = \"terraform-aws-modules/vpc/aws\"\n  version = \"5.1.2\"\n}"
    }
  ],
  keyTakeaway: "Pin module sources to immutable references. Floating references are a ticking time bomb.",
  references: ["https://developer.hashicorp.com/terraform/language/modules/sources"]
},

"terraform-hcl.terraform-fundamentals-repo.are-external-module-refs-pinned-to-immutable-versions-tags-commits-not-f": {
  whatItMeans: "External module references should be pinned to specific version numbers, git tags, or commit SHAs, never to branch names or floating references.",
  whyItMatters: "A floating reference like ref=main means your infrastructure can change behavior without any code change in your repository. This breaks reproducibility and auditability.",
  howToVerify: "Search for module blocks with git sources. Do they use ref= with a tag or commit SHA? For registry modules, is a version constraint specified?",
  exampleComment: "This external module reference uses ref=develop which is a moving target. Please pin to a specific release tag.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "module \"lambda\" {\n  source = \"git::https://github.com/org/tf-lambda.git?ref=develop\"\n}\n\nmodule \"s3\" {\n  source  = \"terraform-aws-modules/s3-bucket/aws\"\n  # No version pinned\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "module \"lambda\" {\n  source = \"git::https://github.com/org/tf-lambda.git?ref=v1.4.0\"\n}\n\nmodule \"s3\" {\n  source  = \"terraform-aws-modules/s3-bucket/aws\"\n  version = \"~> 3.15.0\"\n}"
    }
  ],
  keyTakeaway: "Immutable references ensure your infrastructure is reproducible and auditable.",
  references: []
},

"terraform-hcl.terraform-fundamentals-repo.is-there-a-clear-separation-between-shared-foundations-and-app-specific": {
  whatItMeans: "Shared infrastructure (VPCs, DNS, IAM foundations) should be managed separately from application-specific infrastructure to allow independent lifecycle management.",
  whyItMatters: "Coupling shared foundations with app-specific infra means that deploying an app change could accidentally modify shared networking or IAM. Separation enables independent blast radius control.",
  howToVerify: "Check whether shared resources (VPC, DNS zones, shared IAM roles) are in separate state files from application resources. Are they managed by different root modules?",
  exampleComment: "The VPC and application ECS services are in the same state file. If we need to update the VPC, we risk affecting the application. Consider separating these into independent root modules.",
  codeExamples: [],
  keyTakeaway: "Shared foundations and app-specific infra should have separate lifecycles and separate state.",
  references: []
},

"terraform-hcl.terraform-fundamentals-repo.are-deprecated-resources-modules-removed-instead-of-left-orphaned": {
  whatItMeans: "When resources or modules are no longer needed, they should be properly destroyed and removed from code rather than left as orphaned definitions.",
  whyItMatters: "Orphaned code confuses future developers, wastes cloud spend on unused resources, and increases the attack surface. It also makes plans noisier and harder to review.",
  howToVerify: "Look for commented-out resources, modules that are not referenced, or resources that the plan shows as unchanged but serve no current purpose.",
  exampleComment: "The old bastion host module is still in the code but the comment says we migrated to SSM. Can we remove the module and destroy the bastion to avoid paying for an unused instance?",
  codeExamples: [],
  keyTakeaway: "Delete what you do not use. Orphaned infrastructure costs money and creates confusion.",
  references: []
},

"terraform-hcl.terraform-fundamentals-repo.are-comments-focused-on-intent-tradeoffs-rather-than-restating-code": {
  whatItMeans: "Comments in Terraform code should explain why a decision was made or what tradeoffs were considered, not restate what the HCL already says.",
  whyItMatters: "Comments that repeat the code add noise without value. Comments explaining intent and tradeoffs are invaluable for future reviewers and maintainers.",
  howToVerify: "Read comments in the diff. Do they explain reasoning, constraints, or tradeoffs? Or do they just restate the resource type and attributes?",
  exampleComment: "The comment 'Create an S3 bucket' above the aws_s3_bucket resource is not helpful since the code already says that. A better comment would explain why this bucket exists or any non-obvious configuration choices.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "# Create an S3 bucket for logs\nresource \"aws_s3_bucket\" \"logs\" {\n  bucket = \"my-app-logs\"\n}\n\n# Set the bucket ACL to private\nresource \"aws_s3_bucket_acl\" \"logs\" {\n  bucket = aws_s3_bucket.logs.id\n  acl    = \"private\"\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "# Access logs must be retained for 90 days per SOC2 requirements.\n# Separate bucket from application data to allow independent lifecycle.\nresource \"aws_s3_bucket\" \"logs\" {\n  bucket = \"my-app-logs\"\n}"
    }
  ],
  keyTakeaway: "Comment the why, not the what. Future maintainers need your reasoning, not a narration of the code.",
  references: []
},

// ============================================================
// PROVIDERS & VERSIONING (items 25-38)
// ============================================================

"terraform-hcl.providers-versioning.are-all-providers-declared-under-required-providers-with-source-and-vers": {
  whatItMeans: "Every provider used in the configuration should be declared in the required_providers block with an explicit source address and version constraint.",
  whyItMatters: "Without explicit source and version, Terraform may download an unexpected provider version or even the wrong provider entirely. This is especially risky for community providers with name collisions.",
  howToVerify: "Check the terraform.required_providers block. Does every provider used in resource or data blocks have a corresponding entry with source and version?",
  exampleComment: "The aws provider is used but not declared in required_providers. Please add it with a version constraint to ensure reproducible builds.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "terraform {\n  # No required_providers block\n}\n\nprovider \"aws\" {\n  region = \"us-east-1\"\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "terraform {\n  required_providers {\n    aws = {\n      source  = \"hashicorp/aws\"\n      version = \"~> 5.0\"\n    }\n    random = {\n      source  = \"hashicorp/random\"\n      version = \"~> 3.5\"\n    }\n  }\n}"
    }
  ],
  keyTakeaway: "Declare every provider explicitly. Implicit provider resolution is a source of subtle, hard-to-debug issues.",
  references: ["https://developer.hashicorp.com/terraform/language/providers/requirements"]
},

"terraform-hcl.providers-versioning.are-provider-version-constraints-narrow-enough-for-reproducibility-but-w": {
  whatItMeans: "Provider version constraints should be tight enough to prevent unexpected breaking changes but flexible enough to receive patch updates automatically.",
  whyItMatters: "Too loose (>= 5.0) risks pulling a breaking major version. Too tight (= 5.31.0) blocks security patches. The pessimistic constraint operator (~>) offers a good balance.",
  howToVerify: "Check version constraints in required_providers. Are they using ~> for minor version flexibility? Are they avoiding unconstrained >= or exact = pins?",
  exampleComment: "The constraint >= 5.0 for the AWS provider is too loose and could pull a future breaking version. Consider using ~> 5.31 to allow patch updates while preventing major version jumps.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "aws = {\n  source  = \"hashicorp/aws\"\n  version = \">= 4.0\"  # could pull 5.x, 6.x...\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "aws = {\n  source  = \"hashicorp/aws\"\n  version = \"~> 5.31\"  # allows 5.31.x patches, blocks 6.0\n}"
    }
  ],
  keyTakeaway: "Use ~> for provider versions. It is the sweet spot between reproducibility and patch flexibility.",
  references: ["https://developer.hashicorp.com/terraform/language/expressions/version-constraints"]
},

"terraform-hcl.providers-versioning.is-provider-aliasing-used-correctly-for-multi-account-multi-region-patte": {
  whatItMeans: "When deploying to multiple regions or accounts, provider aliases should be used to explicitly target each region or account rather than relying on default provider behavior.",
  whyItMatters: "Without aliases, resources may accidentally be created in the wrong region or account. Aliases make the target explicit and reviewable.",
  howToVerify: "If the configuration targets multiple regions or accounts, check that provider aliases are defined and that resources explicitly reference the correct alias.",
  exampleComment: "This configuration creates resources in us-east-1 and eu-west-1 but only has one provider block. Please add an aliased provider for the EU region to make the targeting explicit.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "provider \"aws\" {\n  region = \"us-east-1\"\n}\n\n# Oops - this goes to us-east-1, not eu-west-1\nresource \"aws_s3_bucket\" \"eu_data\" {\n  bucket = \"eu-data-bucket\"\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "provider \"aws\" {\n  region = \"us-east-1\"\n}\n\nprovider \"aws\" {\n  alias  = \"eu\"\n  region = \"eu-west-1\"\n}\n\nresource \"aws_s3_bucket\" \"eu_data\" {\n  provider = aws.eu\n  bucket   = \"eu-data-bucket\"\n}"
    }
  ],
  keyTakeaway: "Use provider aliases to make multi-region and multi-account targeting explicit and reviewable.",
  references: ["https://developer.hashicorp.com/terraform/language/providers/configuration#alias-multiple-provider-configurations"]
},

"terraform-hcl.providers-versioning.are-aliased-providers-explicitly-passed-into-child-modules-where-require": {
  whatItMeans: "When a child module needs a specific aliased provider, it must be explicitly passed via the providers argument in the module block.",
  whyItMatters: "If aliased providers are not explicitly passed, child modules inherit only the default (unaliased) provider, which may target the wrong region or account.",
  howToVerify: "Check module calls that target non-default regions or accounts. Is the providers argument used to pass the correct aliased provider?",
  exampleComment: "This module is supposed to create resources in eu-west-1 but the aliased provider is not passed. The module will use the default us-east-1 provider instead.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "module \"eu_bucket\" {\n  source = \"./modules/s3\"\n  # Missing providers argument - uses default region\n  bucket_name = \"eu-data\"\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "module \"eu_bucket\" {\n  source = \"./modules/s3\"\n  providers = {\n    aws = aws.eu\n  }\n  bucket_name = \"eu-data\"\n}"
    }
  ],
  keyTakeaway: "Always pass aliased providers explicitly to child modules. Implicit inheritance causes silent misdeployments.",
  references: ["https://developer.hashicorp.com/terraform/language/modules/develop/providers"]
},

"terraform-hcl.providers-versioning.are-provider-credentials-sourced-securely-env-vars-workload-identity-not": {
  whatItMeans: "Provider authentication should use environment variables, workload identity, or instance profiles rather than hardcoded credentials in HCL files.",
  whyItMatters: "Hardcoded credentials in code are a critical security vulnerability. They end up in version control, plan outputs, and CI logs, and are difficult to rotate.",
  howToVerify: "Search provider blocks for access_key, secret_key, token, or credentials attributes. Check that authentication is documented to use environment variables or IAM roles.",
  exampleComment: "The AWS provider has access_key and secret_key hardcoded. Please remove these and use environment variables or an IAM instance profile instead.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "provider \"aws\" {\n  region     = \"us-east-1\"\n  access_key = \"AKIAIOSFODNN7EXAMPLE\"\n  secret_key = \"wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY\"\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "provider \"aws\" {\n  region = \"us-east-1\"\n  # Credentials sourced from:\n  # - AWS_ACCESS_KEY_ID / AWS_SECRET_ACCESS_KEY env vars\n  # - IAM instance profile\n  # - OIDC workload identity in CI\n}"
    }
  ],
  keyTakeaway: "Never hardcode credentials. Use environment variables, workload identity, or instance profiles.",
  references: ["https://developer.hashicorp.com/terraform/language/providers/configuration"]
},

"terraform-hcl.providers-versioning.are-default-provider-regions-accounts-explicit-to-avoid-accidental-deplo": {
  whatItMeans: "The default provider region and account should be explicitly set rather than relying on environment defaults, to prevent accidentally deploying to the wrong target.",
  whyItMatters: "If the region is not set in the provider block, Terraform falls back to environment variables or CLI defaults, which may differ between developers, causing resources to land in unexpected regions.",
  howToVerify: "Check provider blocks for explicit region settings. Is there any reliance on AWS_DEFAULT_REGION or similar environment variables for production deployments?",
  exampleComment: "The AWS provider does not specify a region. If a developer has AWS_DEFAULT_REGION set differently, resources could be created in the wrong region. Please set region explicitly.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "provider \"aws\" {\n  # Region comes from AWS_DEFAULT_REGION env var\n  # Different developers may have different defaults\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "provider \"aws\" {\n  region = var.aws_region  # Explicit, injected per environment\n}"
    }
  ],
  keyTakeaway: "Explicit provider configuration beats implicit defaults. Do not rely on environment variables for critical settings.",
  references: []
},

"terraform-hcl.providers-versioning.is-terraform-lock-hcl-committed-and-updated-intentionally": {
  whatItMeans: "The .terraform.lock.hcl file records exact provider versions and hashes. It should be committed to version control and only updated intentionally when upgrading providers.",
  whyItMatters: "Without the lock file, different team members may resolve different provider versions, leading to inconsistent plans. The lock file ensures everyone uses identical provider binaries.",
  howToVerify: "Check that .terraform.lock.hcl is committed and not in .gitignore. If it changed, does the change correspond to an intentional provider upgrade?",
  exampleComment: "The lock file changed but no provider versions were updated in required_providers. Is this intentional, or did someone run terraform init on a different platform?",
  codeExamples: [],
  keyTakeaway: "Commit .terraform.lock.hcl. It is your guarantee that everyone runs the same provider versions.",
  references: ["https://developer.hashicorp.com/terraform/language/files/dependency-lock"]
},

"terraform-hcl.providers-versioning.does-lockfile-churn-match-actual-provider-module-changes": {
  whatItMeans: "Changes to .terraform.lock.hcl should correspond to actual provider or module version changes in the configuration, not unexplained hash updates.",
  whyItMatters: "Unexplained lock file churn can indicate that init was run on a different OS/architecture, corrupted downloads, or accidental provider upgrades. Each lock file change deserves scrutiny.",
  howToVerify: "Compare lock file changes with required_providers changes. Do the version bumps match? Are new hashes for the same version explainable (e.g., adding a new platform)?",
  exampleComment: "The lock file shows hash changes for the AWS provider but the version constraint did not change. Was this an intentional re-init or could there be a platform mismatch?",
  codeExamples: [],
  keyTakeaway: "Lockfile changes without corresponding version changes are suspicious. Investigate before approving.",
  references: []
},

"terraform-hcl.providers-versioning.are-provider-upgrades-reviewed-for-breaking-changes-and-state-migrations": {
  whatItMeans: "When upgrading a provider version, the changelog should be reviewed for breaking changes, deprecated resources, and required state migrations.",
  whyItMatters: "Provider upgrades can introduce breaking changes that require code modifications or state migrations. Blindly upgrading can cause plan failures or unexpected resource replacements.",
  howToVerify: "Check if provider versions changed. If so, has the changelog been reviewed? Are there any deprecated resources or attributes that need updating? Is a state migration needed?",
  exampleComment: "This PR bumps the AWS provider from 4.x to 5.x which is a major version change with breaking changes. Please link the migration guide and confirm all deprecated resources have been updated.",
  codeExamples: [],
  keyTakeaway: "Treat provider upgrades like dependency upgrades: read the changelog, test in lower environments, and migrate deliberately.",
  references: ["https://developer.hashicorp.com/terraform/language/upgrade-guides"]
},

"terraform-hcl.providers-versioning.are-beta-preview-providers-features-only-used-with-explicit-risk-acknowl": {
  whatItMeans: "Beta or preview provider features should only be used when the team explicitly accepts the risk of instability, breaking changes, or lack of support.",
  whyItMatters: "Beta features may change behavior without notice, lack documentation, or have bugs. Using them in production without acknowledgment creates hidden risk.",
  howToVerify: "Search for beta or preview provider features in the code. Are they documented with a comment explaining the risk acceptance? Are they isolated from critical infrastructure?",
  exampleComment: "This uses the azurerm provider's preview feature for container apps. Since preview features can change without notice, please add a comment acknowledging the risk and limit usage to non-production.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "resource \"azurerm_container_app\" \"preview_feature\" {\n  # Using preview feature without acknowledgment\n  name = \"my-app\"\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "# RISK: Uses AzureRM preview feature for container apps.\n# Accepted by: @platform-team on 2024-03-15\n# Tracking: INFRA-5678 - migrate when GA\nresource \"azurerm_container_app\" \"preview_feature\" {\n  name = \"my-app\"\n}"
    }
  ],
  keyTakeaway: "Beta features need explicit risk acceptance. Document who approved it and track the migration to GA.",
  references: []
},

"terraform-hcl.providers-versioning.are-provider-level-default-tags-labels-configured-consistently-where-sup": {
  whatItMeans: "Providers like AWS support default_tags in the provider block, which automatically applies tags to all resources. This should be configured consistently for cost allocation and ownership tracking.",
  whyItMatters: "Without default tags, every resource needs individual tag blocks, which are easy to forget. Default tags ensure consistent tagging across all resources with minimal effort.",
  howToVerify: "Check the provider block for default_tags (AWS) or equivalent. Are environment, team, and cost-center tags applied consistently?",
  exampleComment: "The AWS provider does not configure default_tags. Adding default tags for environment, team, and managed-by would ensure consistent tagging across all resources.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "provider \"aws\" {\n  region = \"us-east-1\"\n  # No default tags - each resource must be tagged individually\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "provider \"aws\" {\n  region = \"us-east-1\"\n\n  default_tags {\n    tags = {\n      Environment = var.environment\n      Team        = var.team\n      ManagedBy   = \"terraform\"\n      Repository  = var.repository\n    }\n  }\n}"
    }
  ],
  keyTakeaway: "Use provider-level default tags to enforce consistent tagging without per-resource effort.",
  references: ["https://registry.terraform.io/providers/hashicorp/aws/latest/docs#default_tags"]
},

"terraform-hcl.providers-versioning.are-parallel-provider-blocks-avoiding-conflicting-default-behavior": {
  whatItMeans: "When multiple provider blocks exist (with aliases), their default behaviors should not conflict with each other, especially around default tags, assume_role, or region settings.",
  whyItMatters: "Conflicting provider defaults can cause subtle bugs where resources get unexpected tags, land in wrong regions, or assume the wrong IAM role. Each provider block should have clear, non-overlapping intent.",
  howToVerify: "Review all provider blocks. Do aliased providers have distinct and non-conflicting configurations? Are default_tags or assume_role blocks correctly scoped?",
  exampleComment: "Both provider blocks set different default_tags but some resources do not specify which provider to use. This could lead to inconsistent tagging depending on implicit provider selection.",
  codeExamples: [],
  keyTakeaway: "When using multiple provider blocks, ensure each has clear, non-conflicting defaults.",
  references: []
},

"terraform-hcl.providers-versioning.are-provider-specific-timeouts-retries-configured-when-defaults-are-know": {
  whatItMeans: "Some provider operations (like creating large databases or complex networking setups) take longer than the default timeouts. Custom timeouts and retry settings should be configured when defaults are insufficient.",
  whyItMatters: "Default timeouts that are too short cause false failures during apply, leading to partially applied infrastructure and manual cleanup. Proper timeouts prevent unnecessary failures.",
  howToVerify: "Check resources known to have long provisioning times (RDS, EKS, CloudFront). Do they have custom timeout blocks? Has the team documented known slow resources?",
  exampleComment: "RDS cluster creation can take 15-20 minutes but the default timeout is 10 minutes. Please add a timeout block with a create timeout of at least 30 minutes.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "resource \"aws_rds_cluster\" \"main\" {\n  cluster_identifier = \"main-db\"\n  engine             = \"aurora-postgresql\"\n  # Default timeouts may not be enough\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "resource \"aws_rds_cluster\" \"main\" {\n  cluster_identifier = \"main-db\"\n  engine             = \"aurora-postgresql\"\n\n  timeouts {\n    create = \"45m\"\n    update = \"45m\"\n    delete = \"30m\"\n  }\n}"
    }
  ],
  keyTakeaway: "Configure timeouts for slow-provisioning resources. A false timeout failure is worse than waiting a few extra minutes.",
  references: ["https://developer.hashicorp.com/terraform/language/resources/syntax#operation-timeouts"]
},

"terraform-hcl.providers-versioning.are-provider-deprecations-in-plan-output-addressed-proactively": {
  whatItMeans: "Deprecation warnings in plan output should be addressed proactively by migrating to the recommended replacement rather than ignoring them until they become errors.",
  whyItMatters: "Deprecated attributes and resources will eventually be removed in future provider versions. Ignoring deprecation warnings creates a growing migration debt that blocks provider upgrades.",
  howToVerify: "Check plan output for deprecation warnings. Are they tracked and prioritized for remediation? Is there a ticket for migrating deprecated resources?",
  exampleComment: "The plan shows deprecation warnings for aws_s3_bucket ACL attributes. These will be removed in a future provider version. Please migrate to aws_s3_bucket_acl resources.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "resource \"aws_s3_bucket\" \"data\" {\n  bucket = \"my-data\"\n  acl    = \"private\"  # Deprecated in AWS provider v4+\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "resource \"aws_s3_bucket\" \"data\" {\n  bucket = \"my-data\"\n}\n\nresource \"aws_s3_bucket_acl\" \"data\" {\n  bucket = aws_s3_bucket.data.id\n  acl    = \"private\"\n}"
    }
  ],
  keyTakeaway: "Address deprecation warnings now. They are future errors in disguise.",
  references: []
},

// ============================================================
// STATE, BACKEND & WORKSPACES (items 39-54)
// ============================================================

"terraform-hcl.state-backend-workspaces.is-remote-state-configured-not-local-state-for-shared-team-environments": {
  whatItMeans: "Terraform state should be stored in a remote backend (S3, GCS, Terraform Cloud, etc.) rather than local files for any shared or team environment.",
  whyItMatters: "Local state cannot be shared between team members, has no locking, and is easily lost. Remote state enables collaboration, locking, and recovery.",
  howToVerify: "Check the terraform backend block. Is it configured for a remote backend? Is local state only used for personal experimentation?",
  exampleComment: "This module uses local state (no backend block). For team environments, please configure a remote backend with locking to prevent concurrent modifications.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "terraform {\n  # No backend block = local state file\n  required_version = \">= 1.5.0\"\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "terraform {\n  required_version = \">= 1.5.0\"\n\n  backend \"s3\" {\n    bucket         = \"my-terraform-state\"\n    key            = \"prod/networking/terraform.tfstate\"\n    region         = \"us-east-1\"\n    encrypt        = true\n    dynamodb_table = \"terraform-locks\"\n  }\n}"
    }
  ],
  keyTakeaway: "Remote state is non-negotiable for team environments. Local state is a single point of failure.",
  references: ["https://developer.hashicorp.com/terraform/language/settings/backends/configuration"]
},

"terraform-hcl.state-backend-workspaces.is-backend-storage-encrypted-at-rest": {
  whatItMeans: "The storage backend for Terraform state should have encryption at rest enabled since state files contain sensitive information including resource IDs, passwords, and configuration details.",
  whyItMatters: "Terraform state often contains secrets in plaintext. Without encryption at rest, anyone with access to the storage backend can read sensitive values from the state file.",
  howToVerify: "Check the backend configuration for encrypt = true (S3) or equivalent encryption settings. Verify the underlying storage bucket or account has encryption enabled.",
  exampleComment: "The S3 backend does not set encrypt = true. State files contain sensitive data and should be encrypted at rest.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "backend \"s3\" {\n  bucket = \"tf-state\"\n  key    = \"terraform.tfstate\"\n  region = \"us-east-1\"\n  # No encryption\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "backend \"s3\" {\n  bucket         = \"tf-state\"\n  key            = \"terraform.tfstate\"\n  region         = \"us-east-1\"\n  encrypt        = true\n  kms_key_id     = \"alias/terraform-state\"\n}"
    }
  ],
  keyTakeaway: "Encrypt state at rest. It contains secrets whether you intended it to or not.",
  references: []
},

"terraform-hcl.state-backend-workspaces.is-backend-access-least-privilege-and-scoped-to-required-principals-only": {
  whatItMeans: "Access to the state backend should be restricted to only the principals (users, roles, service accounts) that need it, following least-privilege principles.",
  whyItMatters: "State files contain sensitive infrastructure details. Overly permissive access to state backends can lead to unauthorized reads of secrets or unauthorized modifications of infrastructure.",
  howToVerify: "Review the IAM policies and bucket policies for the state backend. Is access limited to CI/CD runners and authorized operators? Are read and write permissions separated where appropriate?",
  exampleComment: "The state bucket policy allows all IAM users in the account to read state. Please restrict to the Terraform CI role and the infrastructure team role only.",
  codeExamples: [],
  keyTakeaway: "Treat state backend access like database admin access. Least privilege, always.",
  references: []
},

"terraform-hcl.state-backend-workspaces.is-state-locking-enabled-and-reliable-e-g-dynamodb-gcs-locks-remote-back": {
  whatItMeans: "State locking prevents concurrent operations from corrupting the state file. It should be enabled via DynamoDB (AWS), GCS native locking, or backend-native locking mechanisms.",
  whyItMatters: "Without state locking, two simultaneous terraform apply operations can corrupt the state file, potentially causing resource duplication, orphaned resources, or data loss.",
  howToVerify: "Check the backend configuration for locking support. For S3, is a dynamodb_table specified? For GCS, is native locking enabled? For Terraform Cloud, locking is automatic.",
  exampleComment: "The S3 backend does not specify a dynamodb_table for state locking. Without locking, concurrent applies can corrupt state. Please add a DynamoDB table for locking.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "backend \"s3\" {\n  bucket  = \"tf-state\"\n  key     = \"terraform.tfstate\"\n  region  = \"us-east-1\"\n  encrypt = true\n  # No locking configured\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "backend \"s3\" {\n  bucket         = \"tf-state\"\n  key            = \"terraform.tfstate\"\n  region         = \"us-east-1\"\n  encrypt        = true\n  dynamodb_table = \"terraform-state-locks\"\n}"
    }
  ],
  keyTakeaway: "State locking is not optional. It is the difference between a recoverable mistake and a catastrophic one.",
  references: ["https://developer.hashicorp.com/terraform/language/settings/backends/s3"]
},

"terraform-hcl.state-backend-workspaces.is-backend-key-path-naming-deterministic-and-environment-safe": {
  whatItMeans: "The state file key or path should follow a deterministic naming convention that includes the environment, region, and component to prevent state collisions.",
  whyItMatters: "A poorly named state key can lead to two environments sharing the same state file, causing one apply to overwrite another environment's state. This is one of the most devastating Terraform mistakes.",
  howToVerify: "Check the backend key or path. Does it include environment, region, and component identifiers? Is it impossible for two different environments to resolve to the same path?",
  exampleComment: "The backend key is just 'terraform.tfstate'. If dev and prod share the same bucket, they will overwrite each other. Please include the environment in the key path.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "backend \"s3\" {\n  bucket = \"tf-state\"\n  key    = \"terraform.tfstate\"  # No env/component differentiation\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "backend \"s3\" {\n  bucket = \"tf-state\"\n  key    = \"prod/us-east-1/networking/terraform.tfstate\"\n  region = \"us-east-1\"\n}"
    }
  ],
  keyTakeaway: "State key naming should make it impossible for two environments to collide. Be explicit and deterministic.",
  references: []
},

"terraform-hcl.state-backend-workspaces.are-workspaces-used-deliberately-and-not-as-a-substitute-for-proper-envi": {
  whatItMeans: "Terraform workspaces should be used intentionally for their intended purpose, not as a way to avoid proper environment isolation with separate backend configurations.",
  whyItMatters: "Workspaces share the same backend and configuration. Using them as a poor substitute for environment isolation can lead to accidental cross-environment changes and makes it harder to enforce different permissions per environment.",
  howToVerify: "If workspaces are used, check whether they are appropriate for the use case. Would separate root modules with distinct backends provide better isolation?",
  exampleComment: "Using workspaces for dev/staging/prod means all environments share the same backend permissions. Consider separate root modules per environment for stronger isolation.",
  codeExamples: [],
  keyTakeaway: "Workspaces are not environments. Use them for what they are good at (e.g., per-developer sandboxes), not as environment substitutes.",
  references: ["https://developer.hashicorp.com/terraform/cli/workspaces"]
},

"terraform-hcl.state-backend-workspaces.is-there-a-strategy-to-prevent-cross-environment-state-collisions": {
  whatItMeans: "There should be a clear strategy ensuring that different environments (dev, staging, prod) cannot accidentally read from or write to each other's state files.",
  whyItMatters: "Cross-environment state collisions can cause one environment's apply to modify another environment's resources, potentially destroying production infrastructure.",
  howToVerify: "Review backend configuration across environments. Are keys or paths environment-specific? Are bucket policies or IAM roles scoped per environment? Is there any way two environments could share a state path?",
  exampleComment: "Dev and prod use the same S3 bucket with keys differentiated only by a variable. If the variable is misconfigured, they could collide. Consider using separate buckets or bucket prefixes with IAM enforcement.",
  codeExamples: [],
  keyTakeaway: "Cross-environment state collision is a catastrophic failure mode. Design your backend to make it structurally impossible.",
  references: []
},

"terraform-hcl.state-backend-workspaces.are-sensitive-values-prevented-from-landing-in-state-where-avoidable": {
  whatItMeans: "Terraform state stores all resource attributes, including sensitive values. Where possible, sensitive values should be managed outside of Terraform or marked as sensitive to limit exposure.",
  whyItMatters: "Anyone with state file access can read database passwords, API keys, and other secrets stored in plaintext in the state. Minimizing sensitive data in state reduces the blast radius of a state file leak.",
  howToVerify: "Check for resources that create secrets (random_password, aws_secretsmanager_secret_version). Are they marked sensitive? Could the secret management be handled outside Terraform?",
  exampleComment: "The database password is generated by random_password and stored in state as plaintext. Consider using AWS Secrets Manager with a rotation lambda instead, or at minimum mark the output as sensitive.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "resource \"random_password\" \"db\" {\n  length = 32\n}\n\noutput \"db_password\" {\n  value = random_password.db.result  # Plaintext in state and output\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "resource \"random_password\" \"db\" {\n  length = 32\n}\n\noutput \"db_password\" {\n  value     = random_password.db.result\n  sensitive = true  # Prevents showing in plan/output\n}"
    }
  ],
  keyTakeaway: "Assume state files will be read. Minimize secrets in state and mark what remains as sensitive.",
  references: []
},

"terraform-hcl.state-backend-workspaces.is-state-snapshot-versioning-enabled-for-recovery": {
  whatItMeans: "The state backend storage should have versioning enabled so that previous state file versions can be recovered if the current state becomes corrupted.",
  whyItMatters: "State corruption from a failed apply, a bug, or a concurrent write can be recovered quickly if previous versions are available. Without versioning, recovery may require manual state reconstruction.",
  howToVerify: "Check that the S3 bucket or GCS bucket used for state storage has versioning enabled. Can the team restore a previous state version if needed?",
  exampleComment: "The state bucket does not have versioning enabled. Please enable it so we can recover from state corruption without manual reconstruction.",
  codeExamples: [],
  keyTakeaway: "Enable versioning on state storage. It is your safety net for state corruption.",
  references: []
},

"terraform-hcl.state-backend-workspaces.is-state-retention-lifecycle-policy-aligned-with-compliance-and-recovery": {
  whatItMeans: "State file retention and lifecycle policies should balance compliance requirements (keeping history) with cost management (not keeping every version forever).",
  whyItMatters: "Keeping every state version forever wastes storage costs. But deleting too aggressively may violate compliance requirements or remove recovery options. A lifecycle policy should match organizational needs.",
  howToVerify: "Check the S3 or GCS lifecycle policies for the state bucket. Is there a retention policy? Does it align with the organization's compliance and disaster recovery requirements?",
  exampleComment: "The state bucket has no lifecycle policy and retains all versions indefinitely. Consider adding a policy to expire old versions after 90 days to control costs while maintaining a recovery window.",
  codeExamples: [],
  keyTakeaway: "Set a state retention policy that balances compliance, recovery needs, and cost.",
  references: []
},

"terraform-hcl.state-backend-workspaces.are-backend-configuration-changes-migration-safe-and-documented": {
  whatItMeans: "Changes to backend configuration (bucket, key, region) require state migration. These changes should be planned carefully, tested, and documented.",
  whyItMatters: "An incorrect backend migration can lose the state file or split state across two backends. This is a high-risk operation that requires careful execution.",
  howToVerify: "If the backend configuration changed, is there a migration plan? Has terraform init -migrate-state been tested in a lower environment? Are the old and new backends documented?",
  exampleComment: "This PR changes the backend key path. This requires a state migration via terraform init -migrate-state. Please document the migration steps and test in dev first.",
  codeExamples: [],
  keyTakeaway: "Backend changes are state migrations. Plan them like database migrations: carefully, with rollback options.",
  references: ["https://developer.hashicorp.com/terraform/cli/commands/init"]
},

"terraform-hcl.state-backend-workspaces.are-terraform-remote-state-dependencies-minimized-and-versioned-to-reduc": {
  whatItMeans: "Usage of terraform_remote_state data sources to read outputs from other state files should be minimized because they create tight coupling between state files.",
  whyItMatters: "Remote state dependencies create hidden coupling. If the upstream state changes its outputs, downstream consumers can break silently. Minimizing these dependencies reduces fragility.",
  howToVerify: "Search for terraform_remote_state data sources. Are they necessary? Could the data be passed through variables instead? Are the consumed outputs stable and versioned?",
  exampleComment: "This module reads 8 outputs from 3 different remote states. This creates fragile coupling. Consider passing these values as variables from the root module instead.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "data \"terraform_remote_state\" \"vpc\" {\n  backend = \"s3\"\n  config = {\n    bucket = \"tf-state\"\n    key    = \"networking/terraform.tfstate\"\n    region = \"us-east-1\"\n  }\n}\n\n# Tightly coupled to vpc state output names\nresource \"aws_instance\" \"web\" {\n  subnet_id = data.terraform_remote_state.vpc.outputs.private_subnet_ids[0]\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "variable \"vpc_id\" {\n  type        = string\n  description = \"VPC ID from networking stack\"\n}\n\nvariable \"private_subnet_ids\" {\n  type        = list(string)\n  description = \"Private subnet IDs from networking stack\"\n}\n\nresource \"aws_instance\" \"web\" {\n  subnet_id = var.private_subnet_ids[0]\n}"
    }
  ],
  keyTakeaway: "Prefer variables over remote state. Variables are explicit contracts; remote state is implicit coupling.",
  references: []
},

"terraform-hcl.state-backend-workspaces.is-remote-state-data-consumption-scoped-to-needed-outputs-only": {
  whatItMeans: "When using terraform_remote_state, only the specific outputs needed should be consumed rather than pulling in the entire state.",
  whyItMatters: "Reading more outputs than needed increases coupling and makes it harder to change the upstream state. It also exposes more information than necessary to the consuming module.",
  howToVerify: "Check terraform_remote_state references. Are only the needed outputs accessed? Are there references to outputs that are not actually used in the configuration?",
  exampleComment: "This data source reads the entire networking state but only uses vpc_id. Consider documenting which outputs are consumed and removing references to unused outputs.",
  codeExamples: [],
  keyTakeaway: "Consume only what you need from remote state. Less coupling means less breaking.",
  references: []
},

"terraform-hcl.state-backend-workspaces.are-apply-runners-prevented-from-concurrent-applies-against-the-same-sta": {
  whatItMeans: "The CI/CD system should prevent two apply operations from running simultaneously against the same state file, even if state locking is in place.",
  whyItMatters: "While state locking prevents corruption, concurrent applies can still cause confusion: one apply may succeed while the other fails with a lock error, leading to partial infrastructure changes and confusing state.",
  howToVerify: "Check CI/CD pipeline configuration. Is there a concurrency limit per state file or environment? Are parallel applies to the same state prevented at the pipeline level?",
  exampleComment: "Our CI pipeline allows parallel jobs that can trigger concurrent applies to the same state. Please add a concurrency group to ensure only one apply runs per environment at a time.",
  codeExamples: [],
  keyTakeaway: "State locking is a safety net, not a design pattern. Prevent concurrent applies at the pipeline level.",
  references: []
},

"terraform-hcl.state-backend-workspaces.is-state-access-audited-logged-in-cloud-audit-trails": {
  whatItMeans: "Access to state files should be logged and auditable through cloud audit trails (CloudTrail, GCS audit logs, etc.) to track who read or modified state.",
  whyItMatters: "State files contain sensitive infrastructure information. Audit logging helps detect unauthorized access, investigate incidents, and meet compliance requirements.",
  howToVerify: "Check that the state backend bucket has access logging or audit trails enabled. Can the team determine who accessed state files and when?",
  exampleComment: "The state S3 bucket does not have server access logging or CloudTrail data events enabled. Please enable audit logging for compliance and incident investigation.",
  codeExamples: [],
  keyTakeaway: "Log all state access. You need to know who touched your infrastructure state and when.",
  references: []
},

"terraform-hcl.state-backend-workspaces.is-there-a-tested-state-recovery-runbook-for-corruption-or-accidental-de": {
  whatItMeans: "There should be a documented and tested runbook for recovering from state file corruption, accidental deletion, or state drift that the team can execute under pressure.",
  whyItMatters: "State emergencies happen at the worst times. Without a tested runbook, the team will be debugging recovery procedures while production is down. A pre-tested runbook dramatically reduces recovery time.",
  howToVerify: "Ask whether a state recovery runbook exists. Has it been tested recently? Does the team know how to restore from a versioned state backup or recover from a corrupted state?",
  exampleComment: "We do not have a documented state recovery procedure. Given that this PR adds critical production resources, can we create and test a recovery runbook before going live?",
  codeExamples: [],
  keyTakeaway: "A recovery runbook that has never been tested is just a wish. Test it before you need it.",
  references: []
},

// ============================================================
// VARIABLES, LOCALS & OUTPUTS (items 55-76)
// ============================================================

"terraform-hcl.variables-locals-outputs.are-variable-types-explicitly-declared-for-all-inputs": {
  whatItMeans: "Every variable should have an explicit type constraint (string, number, bool, list, map, object) rather than relying on the default any type.",
  whyItMatters: "Explicit types catch misconfiguration early at plan time rather than at apply time. They serve as documentation and prevent passing the wrong type of value.",
  howToVerify: "Check variable blocks in variables.tf. Does every variable have a type attribute? Are complex variables using object types with specific attribute definitions?",
  exampleComment: "This variable has no type declared, so it defaults to any. Please add an explicit type so consumers know what to pass and Terraform can validate it.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "variable \"instance_config\" {\n  description = \"Instance configuration\"\n  # No type - accepts anything\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "variable \"instance_config\" {\n  description = \"Instance configuration\"\n  type = object({\n    instance_type = string\n    ami_id        = string\n    volume_size   = number\n  })\n}"
    }
  ],
  keyTakeaway: "Explicit types are contracts with consumers. They catch errors early and serve as documentation.",
  references: ["https://developer.hashicorp.com/terraform/language/values/variables#type-constraints"]
},

"terraform-hcl.variables-locals-outputs.are-variable-descriptions-clear-and-current": {
  whatItMeans: "Every variable should have a description that clearly explains its purpose, expected values, and any constraints that are not captured by the type alone.",
  whyItMatters: "Descriptions appear in terraform-docs output, Terraform Cloud UI, and IDE hints. Clear descriptions reduce support questions and misconfiguration by module consumers.",
  howToVerify: "Read variable descriptions. Are they helpful and current? Do they explain what the variable controls, not just restate the variable name?",
  exampleComment: "The description for vpc_cidr just says 'The VPC CIDR'. Could you expand it to explain the expected format, valid ranges, and any constraints?",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "variable \"cidr\" {\n  type        = string\n  description = \"The CIDR\"\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "variable \"cidr\" {\n  type        = string\n  description = \"VPC CIDR block in x.x.x.x/y format. Must be /16 to /24. Example: 10.0.0.0/16\"\n}"
    }
  ],
  keyTakeaway: "Write descriptions for someone who has never seen your module. They should understand the variable without reading the code.",
  references: []
},

"terraform-hcl.variables-locals-outputs.are-defaults-safe-and-production-appropriate-or-intentionally-omitted": {
  whatItMeans: "Variable defaults should either be safe for production use or intentionally omitted to force the caller to provide a value. Unsafe defaults should never silently apply to production.",
  whyItMatters: "An unsafe default (like a small instance type or disabled encryption) can silently deploy to production if no override is provided. Omitting the default forces explicit configuration.",
  howToVerify: "Review variable defaults. Would the default value be acceptable in production? If not, should the default be removed to force explicit configuration?",
  exampleComment: "The default instance_type is t3.micro which is fine for dev but dangerous if accidentally applied to production. Consider removing the default so production must explicitly set it.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "variable \"instance_type\" {\n  type    = string\n  default = \"t3.micro\"  # Dangerous if applied to prod by accident\n}\n\nvariable \"enable_encryption\" {\n  type    = bool\n  default = false  # Insecure default\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "variable \"instance_type\" {\n  type        = string\n  description = \"EC2 instance type. No default - must be explicitly set per environment.\"\n  # No default - forces explicit choice\n}\n\nvariable \"enable_encryption\" {\n  type    = bool\n  default = true  # Secure by default\n}"
    }
  ],
  keyTakeaway: "Defaults should be safe. If a default is not safe for production, omit it entirely.",
  references: []
},

"terraform-hcl.variables-locals-outputs.are-validation-blocks-used-for-critical-constraints-cidrs-name-patterns": {
  whatItMeans: "Variable validation blocks should enforce critical constraints like valid CIDR ranges, name patterns, allowed values, and format requirements at plan time.",
  whyItMatters: "Validation blocks catch invalid inputs before Terraform talks to the cloud API, providing clear error messages instead of cryptic API errors. They are especially valuable for shared modules.",
  howToVerify: "Check variables with critical constraints. Do they have validation blocks? Are error messages helpful and actionable?",
  exampleComment: "The cidr_block variable accepts any string but should only accept valid CIDR notation. Please add a validation block to catch invalid values early.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "variable \"environment\" {\n  type = string\n  # No validation - any string accepted\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "variable \"environment\" {\n  type = string\n  validation {\n    condition     = contains([\"dev\", \"staging\", \"prod\"], var.environment)\n    error_message = \"Environment must be one of: dev, staging, prod.\"\n  }\n}\n\nvariable \"vpc_cidr\" {\n  type = string\n  validation {\n    condition     = can(cidrhost(var.vpc_cidr, 0))\n    error_message = \"Must be a valid CIDR block (e.g., 10.0.0.0/16).\"\n  }\n}"
    }
  ],
  keyTakeaway: "Validate inputs at the gate. Clear validation errors beat cryptic API failures every time.",
  references: ["https://developer.hashicorp.com/terraform/language/values/variables#custom-validation-rules"]
},

"terraform-hcl.variables-locals-outputs.are-nullable-optional-variables-handled-intentionally-without-ambiguous": {
  whatItMeans: "Variables with nullable = true or optional() object attributes should handle null values explicitly to prevent unexpected behavior or null-related errors during apply.",
  whyItMatters: "Null values in Terraform can cause subtle bugs: a null value may be silently passed to a resource attribute that does not accept null, causing plan failures or unexpected resource behavior.",
  howToVerify: "Check for nullable variables or optional() type attributes. Is null handled with coalesce, try, or conditional expressions? Are the null semantics documented?",
  exampleComment: "This optional variable defaults to null but is passed directly to a resource attribute that requires a string. Please add a coalesce or conditional to handle the null case.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "variable \"custom_domain\" {\n  type    = string\n  default = null  # What happens when null is used?\n}\n\nresource \"aws_lb_listener_rule\" \"app\" {\n  condition {\n    host_header {\n      values = [var.custom_domain]  # Fails if null\n    }\n  }\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "variable \"custom_domain\" {\n  type        = string\n  default     = null\n  description = \"Optional custom domain. If null, no custom domain rule is created.\"\n}\n\nresource \"aws_lb_listener_rule\" \"app\" {\n  count = var.custom_domain != null ? 1 : 0\n\n  condition {\n    host_header {\n      values = [var.custom_domain]\n    }\n  }\n}"
    }
  ],
  keyTakeaway: "Handle null explicitly. A null variable should never cause a silent failure or unexpected behavior.",
  references: []
},

"terraform-hcl.variables-locals-outputs.are-complex-variable-objects-modeled-to-prevent-invalid-combinations": {
  whatItMeans: "Complex object variables should be structured so that invalid attribute combinations are impossible or caught by validation, not discovered at apply time.",
  whyItMatters: "When a variable object allows contradictory settings (e.g., both public and private networking), the resulting infrastructure may be misconfigured. Good type design prevents invalid states.",
  howToVerify: "Review complex object variable types. Can a consumer pass a combination of attributes that would result in invalid infrastructure? Are preconditions or validations used to prevent this?",
  exampleComment: "This object allows both enable_public_ip = true and subnet_type = private, which is contradictory. Consider restructuring the type or adding a validation to prevent this combination.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "variable \"network_config\" {\n  type = object({\n    enable_public_ip = bool\n    subnet_type      = string  # \"public\" or \"private\"\n    # Nothing prevents public_ip = true + private subnet\n  })\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "variable \"network_config\" {\n  type = object({\n    enable_public_ip = bool\n    subnet_type      = string\n  })\n  validation {\n    condition = (\n      var.network_config.subnet_type == \"public\" ||\n      !var.network_config.enable_public_ip\n    )\n    error_message = \"Cannot enable public IP on a private subnet.\"\n  }\n}"
    }
  ],
  keyTakeaway: "Design variable types that make invalid states impossible. Validation is your second line of defense.",
  references: []
},

"terraform-hcl.variables-locals-outputs.are-locals-used-to-reduce-duplication-and-centralize-computed-intent": {
  whatItMeans: "Local values should centralize computed expressions, naming conventions, and repeated logic so that changes need to be made in only one place.",
  whyItMatters: "Duplicated expressions across resources are error-prone and hard to maintain. Locals centralize logic, making the code DRY and the intent clear.",
  howToVerify: "Look for repeated expressions across resources. Could they be extracted into locals? Are existing locals well-named and serving a clear purpose?",
  exampleComment: "The name tag expression 'format(\"%s-%s-%s\", var.project, var.environment, \"web\")' is repeated in 6 resources. Please extract it into a local to centralize the naming convention.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "resource \"aws_instance\" \"web\" {\n  tags = {\n    Name = \"${var.project}-${var.env}-web\"\n  }\n}\n\nresource \"aws_security_group\" \"web\" {\n  name = \"${var.project}-${var.env}-web-sg\"\n  tags = {\n    Name = \"${var.project}-${var.env}-web-sg\"\n  }\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "locals {\n  name_prefix = \"${var.project}-${var.env}\"\n}\n\nresource \"aws_instance\" \"web\" {\n  tags = {\n    Name = \"${local.name_prefix}-web\"\n  }\n}\n\nresource \"aws_security_group\" \"web\" {\n  name = \"${local.name_prefix}-web-sg\"\n  tags = {\n    Name = \"${local.name_prefix}-web-sg\"\n  }\n}"
    }
  ],
  keyTakeaway: "Use locals to DRY up repeated expressions. One source of truth, many references.",
  references: ["https://developer.hashicorp.com/terraform/language/values/locals"]
},

"terraform-hcl.variables-locals-outputs.are-local-expressions-readable-and-not-overly-clever": {
  whatItMeans: "Local expressions should be readable and straightforward. Overly complex one-liners or deeply nested expressions should be broken into named intermediate steps.",
  whyItMatters: "Clever locals are hard to debug and review. When a local expression requires significant mental parsing, it becomes a maintenance burden and a source of subtle bugs.",
  howToVerify: "Read local expressions. Can you understand what they compute in a single pass? Would breaking them into multiple named locals improve clarity?",
  exampleComment: "This local expression with nested merge, flatten, and conditional logic is hard to follow. Consider breaking it into intermediate locals with descriptive names.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "locals {\n  subnets = { for k, v in merge(\n    { for i, az in var.azs : \"pub-${az}\" => { cidr = cidrsubnet(var.cidr, 8, i), public = true } },\n    { for i, az in var.azs : \"priv-${az}\" => { cidr = cidrsubnet(var.cidr, 8, i + length(var.azs)), public = false } }\n  ) : k => v if v.cidr != \"\" }\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "locals {\n  public_subnets = {\n    for i, az in var.azs :\n    \"pub-${az}\" => {\n      cidr   = cidrsubnet(var.cidr, 8, i)\n      public = true\n    }\n  }\n\n  private_subnets = {\n    for i, az in var.azs :\n    \"priv-${az}\" => {\n      cidr   = cidrsubnet(var.cidr, 8, i + length(var.azs))\n      public = false\n    }\n  }\n\n  all_subnets = merge(local.public_subnets, local.private_subnets)\n}"
    }
  ],
  keyTakeaway: "Break complex locals into named steps. Readability always beats cleverness in infrastructure code.",
  references: []
},

"terraform-hcl.variables-locals-outputs.are-secrets-marked-sensitive-true-on-variables-and-outputs-where-appropr": {
  whatItMeans: "Variables and outputs that contain secrets (passwords, API keys, tokens) should be marked with sensitive = true to prevent their values from appearing in plan output and logs.",
  whyItMatters: "Without the sensitive flag, secret values appear in terraform plan output, CI logs, and PR comments. This exposes credentials to anyone who can view the pipeline or PR.",
  howToVerify: "Search for variables and outputs that handle secrets. Are they marked sensitive = true? Check plan output for any sensitive values appearing in cleartext.",
  exampleComment: "The db_password output is not marked as sensitive. Its value will appear in plan output and could leak in CI logs. Please add sensitive = true.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "variable \"db_password\" {\n  type = string\n  # Missing sensitive flag\n}\n\noutput \"connection_string\" {\n  value = \"postgres://admin:${var.db_password}@${aws_db_instance.main.endpoint}/mydb\"\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "variable \"db_password\" {\n  type      = string\n  sensitive = true\n}\n\noutput \"connection_string\" {\n  value     = \"postgres://admin:${var.db_password}@${aws_db_instance.main.endpoint}/mydb\"\n  sensitive = true\n}"
    }
  ],
  keyTakeaway: "Mark secrets as sensitive. It is a simple flag that prevents accidental exposure in logs and plan output.",
  references: ["https://developer.hashicorp.com/terraform/language/values/variables#suppressing-values-in-cli-output"]
},

"terraform-hcl.variables-locals-outputs.are-outputs-minimal-purposeful-and-non-sensitive-by-default": {
  whatItMeans: "Module outputs should only expose values that downstream consumers actually need. Outputs should not leak sensitive information or expose implementation details unnecessarily.",
  whyItMatters: "Excessive outputs increase the module's coupling surface and may expose sensitive data. Each output is a contract that consumers may depend on, making future changes harder.",
  howToVerify: "Review output blocks. Is each output consumed by a known downstream module or caller? Are there outputs exposing internal details that no consumer needs?",
  exampleComment: "This module exposes 15 outputs but only 3 are consumed downstream. Consider removing unused outputs to reduce coupling and limit information exposure.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "output \"vpc_id\" { value = aws_vpc.main.id }\noutput \"vpc_arn\" { value = aws_vpc.main.arn }\noutput \"vpc_cidr\" { value = aws_vpc.main.cidr_block }\noutput \"vpc_owner_id\" { value = aws_vpc.main.owner_id }\noutput \"vpc_dhcp_options\" { value = aws_vpc.main.dhcp_options_id }\n# 10 more unused outputs..."
    },
    {
      label: "Good",
      language: "hcl",
      code: "output \"vpc_id\" {\n  description = \"The ID of the VPC, consumed by compute and database modules\"\n  value       = aws_vpc.main.id\n}\n\noutput \"private_subnet_ids\" {\n  description = \"Private subnet IDs for workload placement\"\n  value       = aws_subnet.private[*].id\n}"
    }
  ],
  keyTakeaway: "Outputs are contracts. Keep them minimal and purposeful.",
  references: []
},

"terraform-hcl.variables-locals-outputs.are-output-names-stable-and-consumer-friendly": {
  whatItMeans: "Output names should be stable, descriptive, and follow a consistent naming convention that makes sense from the consumer's perspective.",
  whyItMatters: "Renaming an output is a breaking change for all consumers. Stable, descriptive names reduce the need for future renames and make the module interface self-documenting.",
  howToVerify: "Check output names. Are they descriptive and consistent? Do they follow a naming convention? Would renaming them break existing consumers?",
  exampleComment: "The output 'id' is ambiguous when consumed from outside this module. Consider renaming to 'vpc_id' to make it clear from the consumer's perspective.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "# In a VPC module:\noutput \"id\" { value = aws_vpc.main.id }         # Ambiguous\noutput \"subs\" { value = aws_subnet.priv[*].id }  # Abbreviated"
    },
    {
      label: "Good",
      language: "hcl",
      code: "# In a VPC module:\noutput \"vpc_id\" {\n  description = \"VPC identifier\"\n  value       = aws_vpc.main.id\n}\n\noutput \"private_subnet_ids\" {\n  description = \"List of private subnet IDs\"\n  value       = aws_subnet.private[*].id\n}"
    }
  ],
  keyTakeaway: "Name outputs for the consumer, not the author. Stable names prevent breaking changes.",
  references: []
},

"terraform-hcl.variables-locals-outputs.do-output-interface-changes-include-compatibility-notes-for-downstream-m": {
  whatItMeans: "When changing a module's output interface (renaming, removing, or changing types), the PR should document compatibility impacts and migration steps for downstream consumers.",
  whyItMatters: "Output changes can break downstream modules and pipelines silently. Without compatibility notes, consumers discover the breakage during their next plan, causing confusion and delays.",
  howToVerify: "Check if any outputs were renamed, removed, or had their type changed. If so, are there compatibility notes? Are downstream consumers identified and notified?",
  exampleComment: "This PR removes the 'cluster_endpoint' output and replaces it with 'endpoint_url'. Please add a migration note and check if any downstream modules reference the old name.",
  codeExamples: [],
  keyTakeaway: "Output changes are breaking changes. Document them, version them, and notify consumers.",
  references: []
},

"terraform-hcl.variables-locals-outputs.are-environment-specific-values-injected-externally-tfvars-pipeline-vars": {
  whatItMeans: "Environment-specific values (instance sizes, CIDRs, domain names) should be injected via tfvars files or pipeline variables, not hardcoded in the Terraform code.",
  whyItMatters: "Hardcoded environment values prevent code reuse across environments and require code changes for what should be configuration changes. External injection keeps the code DRY and environment-agnostic.",
  howToVerify: "Search for hardcoded environment-specific values in .tf files. Are instance types, CIDRs, domain names, and similar values coming from variables? Are tfvars files per environment?",
  exampleComment: "The AMI ID and instance type are hardcoded in main.tf. Please move these to variables with per-environment tfvars files so the same code works across dev, staging, and prod.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "resource \"aws_instance\" \"web\" {\n  ami           = \"ami-0abcdef1234567890\"  # Hardcoded AMI\n  instance_type = \"m5.xlarge\"               # Hardcoded size\n  subnet_id     = \"subnet-0123456789abcdef0\" # Hardcoded subnet\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "resource \"aws_instance\" \"web\" {\n  ami           = var.ami_id\n  instance_type = var.instance_type\n  subnet_id     = var.subnet_id\n}\n\n# Values come from environments/prod.tfvars:\n# ami_id        = \"ami-0abcdef1234567890\"\n# instance_type = \"m5.xlarge\"\n# subnet_id     = \"subnet-0123456789abcdef0\""
    }
  ],
  keyTakeaway: "Code should be environment-agnostic. All environment-specific values belong in tfvars or pipeline variables.",
  references: []
},

"terraform-hcl.variables-locals-outputs.are-tfvars-files-excluded-from-vcs-if-they-contain-sensitive-or-ephemera": {
  whatItMeans: "Terraform .tfvars files that contain secrets, API keys, or ephemeral values should be excluded from version control via .gitignore.",
  whyItMatters: "Committing sensitive tfvars files to version control exposes secrets in git history permanently. Even if deleted later, the secrets remain in the commit history.",
  howToVerify: "Check .gitignore for sensitive tfvars patterns. Are there any committed tfvars files containing passwords, keys, or tokens? Are non-sensitive tfvars files (like environment configs) appropriately committed?",
  exampleComment: "The secrets.tfvars file contains database passwords and is committed to the repository. Please add it to .gitignore and rotate the exposed credentials.",
  codeExamples: [],
  keyTakeaway: "Sensitive tfvars belong in secret management, not version control. Gitignore them.",
  references: []
},

"terraform-hcl.variables-locals-outputs.are-list-set-map-semantics-chosen-correctly-for-deterministic-plans": {
  whatItMeans: "The choice between list, set, and map types affects how Terraform handles ordering, uniqueness, and plan determinism. The correct type should be chosen based on the use case.",
  whyItMatters: "Using a list when order does not matter can cause unnecessary replacements if the order changes. Using a set loses ordering. Using a map with unstable keys causes replacement churn.",
  howToVerify: "Review variable types and for_each expressions. Are lists used where order matters? Are sets used for unique collections? Are map keys stable?",
  exampleComment: "This variable uses a list of subnet IDs but the order does not matter and could change. Consider using a set(string) to avoid unnecessary replacement when the order changes.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "variable \"allowed_ips\" {\n  type = list(string)  # Order-sensitive but order is meaningless here\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "variable \"allowed_ips\" {\n  type = set(string)  # Order does not matter, uniqueness enforced\n}"
    }
  ],
  keyTakeaway: "Choose collection types intentionally. List for ordered, set for unique unordered, map for keyed lookups.",
  references: ["https://developer.hashicorp.com/terraform/language/expressions/type-constraints"]
},

"terraform-hcl.variables-locals-outputs.are-stringly-typed-booleans-numbers-avoided": {
  whatItMeans: "Variables that represent boolean or numeric values should use bool or number types, not string representations like \"true\" or \"1\".",
  whyItMatters: "Stringly-typed values bypass type checking and lead to subtle bugs. Comparing \"true\" (string) with true (bool) may not behave as expected, and numeric strings lose arithmetic capabilities.",
  howToVerify: "Search for variables typed as string that take values like \"true\", \"false\", \"yes\", \"no\", or numeric strings. Should they be bool or number instead?",
  exampleComment: "The variable enable_logging is typed as string with a default of \"true\". Please change it to bool with a default of true for type safety.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "variable \"enable_monitoring\" {\n  type    = string\n  default = \"true\"  # String, not bool\n}\n\nvariable \"replica_count\" {\n  type    = string\n  default = \"3\"     # String, not number\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "variable \"enable_monitoring\" {\n  type    = bool\n  default = true\n}\n\nvariable \"replica_count\" {\n  type    = number\n  default = 3\n}"
    }
  ],
  keyTakeaway: "Use the right type. Strings that should be bools or numbers are bugs waiting to happen.",
  references: []
},

"terraform-hcl.variables-locals-outputs.are-dynamic-defaults-not-hiding-risky-behavior-in-prod": {
  whatItMeans: "Dynamic defaults that compute different values based on environment or workspace should not silently result in risky configurations in production.",
  whyItMatters: "A default that is safe in dev but risky in prod (like a small instance or disabled encryption) can silently deploy to production if the environment detection logic is wrong.",
  howToVerify: "Check for defaults that use conditionals or lookups based on environment. Would the prod value be safe if the environment detection failed? Is there a validation to catch mismatches?",
  exampleComment: "This default computes the instance type based on terraform.workspace. If the workspace name is wrong, prod could get a t3.micro. Consider making instance_type a required variable with no default for production.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "locals {\n  instance_type = terraform.workspace == \"prod\" ? \"m5.xlarge\" : \"t3.micro\"\n  # If workspace name is misspelled, prod gets t3.micro\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "variable \"instance_type\" {\n  type        = string\n  description = \"Instance type - explicitly set per environment in tfvars\"\n  # No default - must be explicitly set\n}"
    }
  ],
  keyTakeaway: "Dynamic defaults based on environment are fragile. Prefer explicit per-environment configuration.",
  references: []
},

"terraform-hcl.variables-locals-outputs.are-feature-flags-clearly-named-and-fail-safe": {
  whatItMeans: "Boolean variables used as feature flags should have clear names indicating what they enable, and their default values should be the safe/conservative option.",
  whyItMatters: "Poorly named feature flags lead to confusion about what true vs false means. Unsafe defaults mean forgetting to set a flag could enable a risky feature in production.",
  howToVerify: "Check boolean variables that control features. Is the name clear about what true means? Is the default the safe option? Would failing to set the flag produce a safe configuration?",
  exampleComment: "The flag 'public' is ambiguous. Does it make the resource public or check if it is public? Please rename to 'enable_public_access' and default to false.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "variable \"public\" {\n  type    = bool\n  default = true  # Unsafe default - public by default\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "variable \"enable_public_access\" {\n  type        = bool\n  default     = false  # Private by default (safe)\n  description = \"Enable public internet access to the load balancer\"\n}"
    }
  ],
  keyTakeaway: "Feature flags should be clearly named and default to the safe state. When in doubt, default to off.",
  references: []
},

"terraform-hcl.variables-locals-outputs.are-breaking-variable-interface-changes-versioned-or-migration-noted": {
  whatItMeans: "When a module's variable interface changes in a breaking way (renaming, removing, or changing types), the change should be versioned and accompanied by migration notes.",
  whyItMatters: "Breaking variable changes cause all consumers to fail during their next plan. Without versioning and migration notes, consumers face unexpected errors and must reverse-engineer the fix.",
  howToVerify: "Check if any variables were renamed, removed, or had their type changed. Is the module version bumped? Are migration notes included for consumers?",
  exampleComment: "This PR renames instance_count to desired_capacity which is a breaking change. Please bump the module version and add a migration note for consumers.",
  codeExamples: [],
  keyTakeaway: "Breaking interface changes need a version bump and migration guide. Your consumers are your customers.",
  references: []
},

"terraform-hcl.variables-locals-outputs.are-variable-names-concise-domain-specific-and-consistent-across-modules": {
  whatItMeans: "Variable names should be concise, use domain-specific terminology, and follow a consistent naming convention across all modules in the repository.",
  whyItMatters: "Inconsistent naming forces consumers to memorize different conventions per module. Verbose or generic names add noise. Domain-specific names convey meaning efficiently.",
  howToVerify: "Compare variable names across modules. Is the naming consistent? Are names concise but descriptive? Do they use domain terms rather than generic labels?",
  exampleComment: "This module uses 'db_instance_class' while the other database module uses 'rds_instance_type'. Please standardize on one convention across all database modules.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "variable \"the_vpc_cidr_block_for_this_module\" {\n  type = string  # Too verbose\n}\n\nvariable \"x\" {\n  type = number  # Too terse, meaningless\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "variable \"vpc_cidr\" {\n  type        = string\n  description = \"CIDR block for the VPC\"\n}\n\nvariable \"az_count\" {\n  type        = number\n  description = \"Number of availability zones to use\"\n}"
    }
  ],
  keyTakeaway: "Good variable names are concise, consistent, and domain-specific. They should read naturally in context.",
  references: []
},

"terraform-hcl.variables-locals-outputs.are-computed-names-deterministic-and-compliant-with-provider-limits": {
  whatItMeans: "Resource names computed from variables or locals should produce deterministic results and respect cloud provider naming constraints (length limits, character restrictions).",
  whyItMatters: "Names that exceed provider length limits cause apply failures. Non-deterministic names (using timestamp or random) cause unnecessary replacements. Invalid characters cause API errors.",
  howToVerify: "Check computed name expressions. Do they respect length limits? Do they use only allowed characters? Are they deterministic (same inputs = same name)?",
  exampleComment: "This computed name can exceed the 63-character limit for S3 bucket names when project and environment names are long. Please add a substr or validation to prevent this.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "locals {\n  # Can easily exceed 63-char S3 bucket name limit\n  bucket_name = \"${var.company}-${var.project}-${var.environment}-${var.component}-data\"\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "locals {\n  # Truncated to respect S3 naming limits\n  bucket_name = substr(\n    \"${var.project}-${var.env}-data\",\n    0,\n    63\n  )\n}"
    }
  ],
  keyTakeaway: "Computed names must be deterministic and respect provider limits. Test with long inputs.",
  references: []
},

"terraform-hcl.variables-locals-outputs.are-regex-validations-understandable-and-tested-for-edge-cases": {
  whatItMeans: "Regex patterns used in variable validations should be readable, documented, and tested against both valid and invalid edge cases.",
  whyItMatters: "Complex regex patterns are hard to review and may have subtle bugs that accept invalid input or reject valid input. Untested regex can create false confidence in input validation.",
  howToVerify: "Review regex patterns in validation blocks. Are they documented with examples? Can you understand what they match? Have edge cases been considered?",
  exampleComment: "This regex for validating resource names is complex and undocumented. Please add a comment explaining the pattern and examples of valid/invalid inputs.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "variable \"name\" {\n  type = string\n  validation {\n    condition     = can(regex(\"^(?:[a-z](?:[-a-z0-9]{0,61}[a-z0-9])?)$\", var.name))\n    error_message = \"Invalid name.\"\n  }\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "variable \"name\" {\n  type = string\n  validation {\n    # Must start with lowercase letter, contain only lowercase,\n    # digits, and hyphens, end with letter/digit, max 63 chars.\n    # Valid: \"my-app-1\" Invalid: \"-start\", \"Too_Long\", \"UPPER\"\n    condition     = can(regex(\"^[a-z][a-z0-9-]{0,61}[a-z0-9]$\", var.name))\n    error_message = \"Name must be 2-63 chars, start with a letter, end with letter/digit, and contain only lowercase letters, digits, and hyphens.\"\n  }\n}"
    }
  ],
  keyTakeaway: "Document regex patterns with examples. A regex you cannot explain is a regex you should simplify.",
  references: []
},

// ============================================================
// MODULE DESIGN & COMPOSITION (items 77-100)
// ============================================================

"terraform-hcl.module-design-composition.is-each-module-focused-on-one-infra-capability-domain": {
  whatItMeans: "Each module should encapsulate a single infrastructure capability or domain (networking, compute, database) rather than bundling multiple unrelated concerns.",
  whyItMatters: "Multi-concern modules are hard to test, reuse, and maintain. A module that creates both a VPC and an ECS cluster cannot be used by teams that only need networking.",
  howToVerify: "Check the resources in the module. Do they all relate to a single domain? Could the module be described in one sentence without using 'and'?",
  exampleComment: "This module creates a VPC, an RDS cluster, and an ECS service. These are three distinct domains. Consider splitting into separate networking, database, and compute modules.",
  codeExamples: [],
  keyTakeaway: "One module, one job. If you need 'and' to describe what a module does, it does too much.",
  references: []
},

"terraform-hcl.module-design-composition.are-module-inputs-minimal-and-high-signal-rather-than-pass-through-wrapp": {
  whatItMeans: "Module inputs should represent meaningful configuration decisions, not one-to-one wrappers around resource attributes that just pass values through.",
  whyItMatters: "Pass-through wrappers add indirection without value. They make the module harder to use and harder to maintain.",
  howToVerify: "Check if module variables map directly to single resource attributes without transformation. If removing the module and using the resource directly would be simpler, the module may not be adding value.",
  exampleComment: "This module has 25 variables that each map 1:1 to an RDS attribute. Consider exposing higher-level abstractions like 'size' (small/medium/large) instead of raw attribute passthrough.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "# Module with 1:1 passthrough variables\nvariable \"allocated_storage\" { type = number }\nvariable \"engine\" { type = string }\nvariable \"engine_version\" { type = string }\nvariable \"instance_class\" { type = string }\nvariable \"storage_type\" { type = string }\n# ... 20 more pass-through variables"
    },
    {
      label: "Good",
      language: "hcl",
      code: "variable \"tier\" {\n  type        = string\n  description = \"Database tier: small, medium, large, xlarge\"\n  validation {\n    condition     = contains([\"small\", \"medium\", \"large\", \"xlarge\"], var.tier)\n    error_message = \"Tier must be small, medium, large, or xlarge.\"\n  }\n}\n\nlocals {\n  tier_configs = {\n    small  = { instance_class = \"db.t3.medium\", storage = 50 }\n    large  = { instance_class = \"db.r5.xlarge\", storage = 500 }\n  }\n}"
    }
  ],
  keyTakeaway: "Modules should provide abstractions, not wrappers. Expose decisions, not knobs.",
  references: []
},

"terraform-hcl.module-design-composition.are-module-outputs-limited-to-true-consumption-needs": {
  whatItMeans: "Modules should only output values that downstream consumers actually need, not every attribute of every resource.",
  whyItMatters: "Excessive outputs increase the module's coupling surface. Every output is a contract that constrains future refactoring.",
  howToVerify: "Review module outputs. Is each output consumed by a known caller? Could the output list be reduced without breaking any consumer?",
  exampleComment: "This module exposes 20 outputs but only 4 are used downstream. The unused outputs create maintenance burden. Please remove the unused ones.",
  codeExamples: [],
  keyTakeaway: "Output only what consumers need. Every extra output is a promise you have to keep.",
  references: []
},

"terraform-hcl.module-design-composition.are-modules-avoiding-hidden-side-effects-across-unrelated-services": {
  whatItMeans: "Modules should not secretly create or modify resources in unrelated services as side effects.",
  whyItMatters: "Hidden side effects make modules unpredictable. If a networking module secretly creates IAM roles, consumers will be surprised and may create conflicts.",
  howToVerify: "Review all resources created by the module. Are there surprises like IAM roles, DNS records, or monitoring resources that are not obvious from the module name?",
  exampleComment: "This VPC module also creates CloudWatch alarms and SNS topics. These side effects should be in a separate monitoring module.",
  codeExamples: [],
  keyTakeaway: "No surprises. A module should do what its name says, nothing more.",
  references: []
},

"terraform-hcl.module-design-composition.are-reusable-modules-cloud-provider-idiomatic-and-not-leaky-abstractions": {
  whatItMeans: "Reusable modules should follow the idioms and patterns of the target cloud provider rather than creating generic abstractions that obscure provider-specific behavior.",
  whyItMatters: "Leaky abstractions that try to hide cloud differences often fail at edge cases, confuse users familiar with the provider, and prevent access to provider-specific features.",
  howToVerify: "Check if the module uses cloud-agnostic naming or patterns that hide important provider-specific behavior.",
  exampleComment: "This compute_instance module tries to abstract across AWS and GCP but the abstraction leaks. Consider separate provider-specific modules.",
  codeExamples: [],
  keyTakeaway: "Embrace your cloud provider's idioms. Generic abstractions usually leak and confuse.",
  references: []
},

"terraform-hcl.module-design-composition.are-module-defaults-safe-for-first-time-consumers": {
  whatItMeans: "Module defaults should produce a working, secure, and reasonable configuration when a first-time consumer uses the module with minimal input.",
  whyItMatters: "Unsafe defaults lead to security incidents when consumers trust the module and deploy without reviewing every default. Safe defaults build trust.",
  howToVerify: "Try using the module with only required variables. Would the result be secure and functional?",
  exampleComment: "This module defaults to no encryption and public access. Please make encryption and private access the defaults.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "variable \"enable_encryption\" {\n  type    = bool\n  default = false  # Insecure default\n}\n\nvariable \"publicly_accessible\" {\n  type    = bool\n  default = true   # Dangerous default\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "variable \"enable_encryption\" {\n  type    = bool\n  default = true   # Secure by default\n}\n\nvariable \"publicly_accessible\" {\n  type    = bool\n  default = false  # Private by default\n}"
    }
  ],
  keyTakeaway: "Defaults should be secure and sensible. The path of least resistance should be the safe path.",
  references: []
},

"terraform-hcl.module-design-composition.are-optional-features-toggled-cleanly-with-clear-behavior-when-disabled": {
  whatItMeans: "Optional features within a module should be toggled with clear boolean flags, and the behavior when disabled should be well-defined (no resources created, no errors).",
  whyItMatters: "Unclear feature toggles lead to confusion. If disabling a feature causes errors or leaves orphaned resources, the toggle is broken.",
  howToVerify: "Check feature flag variables. Is the behavior clear when set to false? Are all related resources gated by the same flag?",
  exampleComment: "Setting enable_monitoring = false still creates the CloudWatch dashboard but without metrics, causing errors. Please gate all monitoring resources on the flag.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "# Only alarm is conditional, dashboard always created\nresource \"aws_cloudwatch_metric_alarm\" \"cpu\" {\n  count = var.enable_monitoring ? 1 : 0\n}\n\nresource \"aws_cloudwatch_dashboard\" \"main\" {\n  # Always created - references alarm that may not exist\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "resource \"aws_cloudwatch_metric_alarm\" \"cpu\" {\n  count = var.enable_monitoring ? 1 : 0\n}\n\nresource \"aws_cloudwatch_dashboard\" \"main\" {\n  count = var.enable_monitoring ? 1 : 0\n  # All monitoring resources gated by the same flag\n}"
    }
  ],
  keyTakeaway: "Feature toggles should be all-or-nothing. Partial disabling causes confusing failures.",
  references: []
},

"terraform-hcl.module-design-composition.is-for-each-used-instead-of-count-where-stable-identity-is-required": {
  whatItMeans: "When creating multiple resource instances where each has a meaningful identity, for_each with a map or set should be used instead of count to prevent index-based replacement churn.",
  whyItMatters: "With count, removing an item from the middle of a list causes all subsequent items to shift indices, triggering unnecessary destroys and recreates.",
  howToVerify: "Check resources using count with a list. If removing an item would cause index shifting and resource replacement, for_each would be more appropriate.",
  exampleComment: "These subnets use count with a list of AZs. Removing an AZ from the middle would force-replace all subsequent subnets. Please use for_each keyed by AZ name.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "resource \"aws_subnet\" \"main\" {\n  count      = length(var.subnet_names)\n  cidr_block = cidrsubnet(var.vpc_cidr, 8, count.index)\n  tags = {\n    Name = var.subnet_names[count.index]\n  }\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "resource \"aws_subnet\" \"main\" {\n  for_each   = var.subnets  # map keyed by stable name\n  cidr_block = each.value.cidr\n  tags = {\n    Name = each.key\n  }\n}"
    }
  ],
  keyTakeaway: "Use for_each for stable identities. count is for truly fungible resources.",
  references: ["https://developer.hashicorp.com/terraform/language/meta-arguments/for_each"]
},

"terraform-hcl.module-design-composition.if-count-is-used-is-index-churn-risk-understood-and-accepted": {
  whatItMeans: "When count is used, the team should understand that adding or removing items from the middle of a list causes index-based replacement of subsequent resources.",
  whyItMatters: "Index churn can cause unexpected resource destruction and recreation, potentially causing downtime.",
  howToVerify: "If count is used with a list, is the list append-only? Is the churn risk documented? Would for_each be better?",
  exampleComment: "This resource uses count with a list. Adding or removing items will shift indices and force-replace resources. Is this risk understood?",
  codeExamples: [],
  keyTakeaway: "count is fine for simple on/off toggles. For everything else, prefer for_each.",
  references: []
},

"terraform-hcl.module-design-composition.are-resource-keys-in-for-each-stable-over-time-to-avoid-replacements": {
  whatItMeans: "The keys used in for_each maps should be stable identifiers that do not change over time, since changing a key causes destroy and recreate.",
  whyItMatters: "Unstable keys cause unnecessary replacement churn, potentially destroying stateful resources like databases.",
  howToVerify: "Check for_each map keys. Are they based on stable identifiers? Could they change due to ordering or computation changes?",
  exampleComment: "The for_each key is based on the list index. If the order changes, resources will be replaced. Use a stable name instead.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "resource \"aws_subnet\" \"main\" {\n  for_each = { for i, cidr in var.cidrs : tostring(i) => cidr }\n  # Keys \"0\", \"1\", \"2\" shift when list changes\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "resource \"aws_subnet\" \"main\" {\n  for_each = { for s in var.subnets : s.name => s }\n  # Keys are stable names like \"web\", \"app\", \"db\"\n}"
    }
  ],
  keyTakeaway: "for_each keys are resource identifiers. Use stable, meaningful keys.",
  references: []
},

"terraform-hcl.module-design-composition.are-module-level-depends-on-used-sparingly-and-justified": {
  whatItMeans: "Module-level depends_on should only be used when Terraform cannot infer the dependency from references. It should have a comment explaining why.",
  whyItMatters: "Module-level depends_on forces all resources in the module to wait, slowing applies and creating unnecessary coupling.",
  howToVerify: "Search for depends_on in module blocks. Is the dependency already captured by references? Is there a justifying comment?",
  exampleComment: "This module depends_on forces all 20 resources to wait for the IAM role. Could a specific resource reference capture this dependency instead?",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "module \"app\" {\n  source     = \"./modules/app\"\n  depends_on = [aws_iam_role.app]  # No explanation\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "module \"app\" {\n  source   = \"./modules/app\"\n  role_arn = aws_iam_role.app.arn  # Implicit dependency\n}"
    }
  ],
  keyTakeaway: "Prefer implicit dependencies via references. Use depends_on only as a last resort.",
  references: ["https://developer.hashicorp.com/terraform/language/meta-arguments/depends_on"]
},

"terraform-hcl.module-design-composition.are-dynamic-blocks-used-only-when-they-improve-maintainability": {
  whatItMeans: "Dynamic blocks should be used when they genuinely simplify code by eliminating repetitive blocks, not as a default pattern for every nested block.",
  whyItMatters: "Overuse of dynamic blocks makes code harder to read. A simple static block is easier to understand than a dynamic block generating the same content.",
  howToVerify: "Check dynamic blocks. Would a static block be simpler for fixed iterations? Does the dynamic block genuinely reduce duplication?",
  exampleComment: "This dynamic block generates a single ingress rule. A static block would be simpler and more readable.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "# Dynamic block for a single fixed rule - overkill\ndynamic \"ingress\" {\n  for_each = [{ port = 443 }]\n  content {\n    from_port   = ingress.value.port\n    to_port     = ingress.value.port\n    protocol    = \"tcp\"\n    cidr_blocks = [\"0.0.0.0/0\"]\n  }\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "# Static block for fixed rule\ningress {\n  from_port   = 443\n  to_port     = 443\n  protocol    = \"tcp\"\n  cidr_blocks = [\"0.0.0.0/0\"]\n}\n\n# Dynamic block for variable-length input\ndynamic \"ingress\" {\n  for_each = var.additional_ingress_rules\n  content {\n    from_port   = ingress.value.port\n    to_port     = ingress.value.port\n    protocol    = \"tcp\"\n    cidr_blocks = ingress.value.cidrs\n  }\n}"
    }
  ],
  keyTakeaway: "Use dynamic blocks when iteration genuinely helps. For fixed structures, static blocks are clearer.",
  references: ["https://developer.hashicorp.com/terraform/language/expressions/dynamic-blocks"]
},

"terraform-hcl.module-design-composition.are-data-sources-in-modules-deterministic-and-not-introducing-hidden-ext": {
  whatItMeans: "Data sources within modules should produce deterministic results and not introduce hidden dependencies on external state.",
  whyItMatters: "A data source querying live infrastructure can return different results each time, making the module non-deterministic.",
  howToVerify: "Check data sources in the module. Could they return different results between plan and apply? Are external dependencies documented?",
  exampleComment: "This module uses a data source to look up the latest AMI, which could change between plan and apply. Consider passing the AMI as a variable.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "# Hidden dependency on latest AMI\ndata \"aws_ami\" \"latest\" {\n  most_recent = true\n  owners      = [\"amazon\"]\n  filter {\n    name   = \"name\"\n    values = [\"amzn2-ami-hvm-*\"]\n  }\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "variable \"ami_id\" {\n  type        = string\n  description = \"AMI ID - looked up externally for reproducibility\"\n}"
    }
  ],
  keyTakeaway: "Data sources in modules create hidden external dependencies. Prefer passing values as variables.",
  references: []
},

"terraform-hcl.module-design-composition.are-module-versions-incremented-and-changelogged-for-interface-or-behavi": {
  whatItMeans: "When a module's interface or behavior changes, the version should be incremented following semantic versioning and documented in a changelog.",
  whyItMatters: "Without versioning, consumers cannot control when they adopt changes. Without a changelog, they cannot understand what changed.",
  howToVerify: "Check if the module version was bumped for interface or behavior changes. Is there a CHANGELOG? Does versioning follow semver?",
  exampleComment: "This PR adds a required variable, which is a breaking change, but the version was not bumped. Please tag a new major version.",
  codeExamples: [],
  keyTakeaway: "Version your modules. A changelog is the contract between module authors and consumers.",
  references: ["https://semver.org/"]
},

"terraform-hcl.module-design-composition.are-module-readme-inputs-outputs-autogenerated-or-kept-in-sync": {
  whatItMeans: "Module documentation for inputs and outputs should be auto-generated using terraform-docs or kept manually in sync with every interface change.",
  whyItMatters: "Manual documentation drifts from reality. Auto-generated docs are always accurate.",
  howToVerify: "Check if terraform-docs is configured. If docs are manual, do they match current variables and outputs?",
  exampleComment: "The README shows 10 variables but the module now has 14. Please run terraform-docs to regenerate.",
  codeExamples: [],
  keyTakeaway: "Use terraform-docs for auto-generated documentation. Stale docs are worse than no docs.",
  references: ["https://github.com/terraform-docs/terraform-docs"]
},

"terraform-hcl.module-design-composition.are-examples-tests-covering-representative-module-usage-patterns": {
  whatItMeans: "Modules should have example configurations and tests covering the most common and important usage patterns.",
  whyItMatters: "Examples serve as living documentation and integration test fixtures. Tests catch regressions before they reach consumers.",
  howToVerify: "Check for examples/ or tests/ directories. Do examples cover common patterns? Are there terraform test files?",
  exampleComment: "This module has no examples or tests. Please add a basic example showing common usage.",
  codeExamples: [],
  keyTakeaway: "Examples and tests are the most valuable investment in module quality.",
  references: ["https://developer.hashicorp.com/terraform/language/tests"]
},

"terraform-hcl.module-design-composition.is-ownership-clear-for-each-module-team-service": {
  whatItMeans: "Every module should have a clear owner (team or individual) responsible for maintenance and reviews.",
  whyItMatters: "Unowned modules accumulate tech debt and stop receiving updates. Clear ownership ensures accountability.",
  howToVerify: "Check for CODEOWNERS, README ownership sections, or metadata indicating the module owner.",
  exampleComment: "This module does not have a CODEOWNERS entry. Please add one so the owning team is auto-added as reviewers.",
  codeExamples: [],
  keyTakeaway: "Every module needs an owner. Unowned modules are unmaintained modules.",
  references: []
},

"terraform-hcl.module-design-composition.are-module-dependencies-acyclic-and-understandable": {
  whatItMeans: "Module dependencies should form a directed acyclic graph. Circular dependencies indicate poor design and cause Terraform errors.",
  whyItMatters: "Circular dependencies prevent Terraform from determining execution order and cause plan failures.",
  howToVerify: "Map module dependencies. Does any chain of dependencies form a cycle?",
  exampleComment: "The networking module depends on security which depends on networking. This circular dependency needs restructuring.",
  codeExamples: [],
  keyTakeaway: "Module dependencies should flow in one direction. Cycles indicate a design problem.",
  references: []
},

"terraform-hcl.module-design-composition.are-modules-avoiding-provider-configuration-internally-unless-explicitly": {
  whatItMeans: "Reusable modules should not define provider blocks internally. They should inherit providers from the caller.",
  whyItMatters: "Internal provider configuration prevents callers from controlling provider behavior and causes conflicts with different settings.",
  howToVerify: "Check for provider blocks inside reusable modules. Should the module instead inherit from the caller?",
  exampleComment: "This module defines its own AWS provider with a hardcoded region. Please remove it and let callers pass providers in.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "# Inside reusable module\nprovider \"aws\" {\n  region = \"us-east-1\"  # Hardcoded\n}\n\nresource \"aws_s3_bucket\" \"data\" {\n  bucket = var.name\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "# Module inherits provider from caller\nresource \"aws_s3_bucket\" \"data\" {\n  bucket = var.name\n}\n\n# Caller:\nmodule \"bucket\" {\n  source    = \"./modules/s3\"\n  providers = { aws = aws.eu }\n}"
    }
  ],
  keyTakeaway: "Reusable modules should never configure providers. Let callers control provider settings.",
  references: ["https://developer.hashicorp.com/terraform/language/modules/develop/providers"]
},

"terraform-hcl.module-design-composition.are-nested-module-stacks-not-too-deep-for-maintainability": {
  whatItMeans: "Module nesting should be kept to 2-3 levels to maintain readability and debuggability.",
  whyItMatters: "Deeply nested modules make it hard to trace resource creation and debug issues. Error messages reference deep paths that are hard to navigate.",
  howToVerify: "Trace module call chains. How deep do they go? Can you easily find which module creates a specific resource?",
  exampleComment: "This module call is 5 levels deep. Debugging requires navigating all 5 levels. Consider flattening the hierarchy.",
  codeExamples: [],
  keyTakeaway: "Keep module nesting shallow. Two to three levels is usually sufficient.",
  references: []
},

"terraform-hcl.module-design-composition.are-resource-names-tags-within-modules-customizable-where-needed": {
  whatItMeans: "Resources created by modules should allow callers to customize names and tags, since naming conventions vary across organizations.",
  whyItMatters: "Hardcoded names prevent modules from fitting into different organizational standards and prevent required tags for compliance.",
  howToVerify: "Check if resources have hardcoded names or tags. Can callers pass custom name prefixes and additional tags?",
  exampleComment: "Resource names are hardcoded. Please add a name_prefix variable so callers can customize naming.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "resource \"aws_s3_bucket\" \"data\" {\n  bucket = \"acme-prod-data\"  # Hardcoded\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "resource \"aws_s3_bucket\" \"data\" {\n  bucket = \"${var.name_prefix}-data\"\n  tags   = merge(var.tags, { ManagedBy = \"terraform\" })\n}"
    }
  ],
  keyTakeaway: "Make names and tags customizable via variables.",
  references: []
},

"terraform-hcl.module-design-composition.are-guardrails-encoded-timeouts-protections-validations-in-module-defaul": {
  whatItMeans: "Modules should encode sensible guardrails in defaults: timeouts for slow resources, prevent_destroy for critical resources, and validations for dangerous inputs.",
  whyItMatters: "Guardrails in defaults protect consumers from common mistakes without requiring them to remember protections.",
  howToVerify: "Check if the module sets timeouts, prevent_destroy, and input validations by default.",
  exampleComment: "This database module does not set prevent_destroy by default. A consumer could accidentally destroy the production database.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "resource \"aws_rds_instance\" \"main\" {\n  identifier     = var.name\n  instance_class = var.instance_class\n  # No guardrails\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "resource \"aws_rds_instance\" \"main\" {\n  identifier     = var.name\n  instance_class = var.instance_class\n\n  lifecycle {\n    prevent_destroy = true\n  }\n\n  timeouts {\n    create = \"45m\"\n    update = \"30m\"\n    delete = \"30m\"\n  }\n}"
    }
  ],
  keyTakeaway: "Bake guardrails into module defaults. Protect consumers from themselves.",
  references: []
},

"terraform-hcl.module-design-composition.are-cloud-quotas-limits-considered-in-module-behavior": {
  whatItMeans: "Modules creating multiple resources should consider cloud provider quotas and limits to prevent apply failures.",
  whyItMatters: "Hitting a cloud quota during apply can leave infrastructure partially created. Modules should document quota requirements.",
  howToVerify: "Check if the module creates resources in quantities that could hit quotas. Are quota requirements documented?",
  exampleComment: "This module creates a NAT Gateway per AZ, but the default quota is 5 per region. Add a validation or document the quota.",
  codeExamples: [],
  keyTakeaway: "Document quota requirements. Quota failures mid-apply are painful to recover from.",
  references: []
},

"terraform-hcl.module-design-composition.are-one-off-resources-kept-out-of-shared-modules-when-they-reduce-reuse": {
  whatItMeans: "Resources specific to a single use case should not be added to shared modules where they reduce clarity and reusability.",
  whyItMatters: "One-off resources in shared modules increase complexity for all consumers. They belong in the caller's configuration.",
  howToVerify: "Check if new resources in a shared module are useful for all consumers or just one.",
  exampleComment: "This custom Lambda is specific to the billing team. Consider moving it to the billing team's own configuration instead of the shared module.",
  codeExamples: [],
  keyTakeaway: "Shared modules should serve many consumers. One-off resources belong in the caller.",
  references: []
},

// ============================================================
// RESOURCE SEMANTICS & LIFECYCLE (items 101-124)
// ============================================================

"terraform-hcl.resource-semantics-lifecycle.are-any-planned-destroys-intentional-reviewed-and-approved-for-the-targe": {
  whatItMeans: "Any resource destruction shown in the plan output should be intentional, reviewed, and approved for the specific target environment.",
  whyItMatters: "Unintentional destroys can delete databases and storage with irreversible data loss. Every destroy action deserves explicit scrutiny.",
  howToVerify: "Check the plan for any destroy actions. Are they expected? Has the reviewer confirmed? Are backups in place for stateful resources?",
  exampleComment: "The plan shows 3 resources will be destroyed. Please confirm these are intentional and that data has been backed up.",
  codeExamples: [],
  keyTakeaway: "Every destroy in a plan needs explicit sign-off. Assume it is accidental until proven intentional.",
  references: []
},

"terraform-hcl.resource-semantics-lifecycle.are-critical-resources-protected-with-lifecycle-prevent-destroy-where-ap": {
  whatItMeans: "Critical resources like databases and encryption keys should use lifecycle { prevent_destroy = true } to prevent accidental deletion.",
  whyItMatters: "prevent_destroy causes Terraform to error if a plan would destroy the resource, providing a safety net against accidental deletion.",
  howToVerify: "Check critical resources (databases, S3 buckets, KMS keys) for prevent_destroy. Are stateful resources protected?",
  exampleComment: "This RDS instance does not have prevent_destroy. An accidental destroy could delete the production database.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "resource \"aws_rds_instance\" \"main\" {\n  identifier     = var.db_name\n  engine         = \"postgres\"\n  instance_class = var.instance_class\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "resource \"aws_rds_instance\" \"main\" {\n  identifier     = var.db_name\n  engine         = \"postgres\"\n  instance_class = var.instance_class\n\n  lifecycle {\n    prevent_destroy = true\n  }\n}"
    }
  ],
  keyTakeaway: "Protect critical resources with prevent_destroy. It is the last line of defense.",
  references: ["https://developer.hashicorp.com/terraform/language/meta-arguments/lifecycle#prevent_destroy"]
},

"terraform-hcl.resource-semantics-lifecycle.is-create-before-destroy-used-for-zero-low-downtime-replacement-paths-wh": {
  whatItMeans: "For resources that need replacement, create_before_destroy ensures the new resource is created before the old one is destroyed, minimizing downtime.",
  whyItMatters: "The default destroy-then-create causes downtime during the replacement window. create_before_destroy eliminates this gap.",
  howToVerify: "Check resources that may need replacement. Would downtime during replacement be unacceptable? Is create_before_destroy set?",
  exampleComment: "This security group could be replaced during updates. Without create_before_destroy, there will be a gap. Please add the lifecycle block.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "resource \"aws_security_group\" \"web\" {\n  name = \"web-sg-${var.version}\"\n  # Default: destroy then create = downtime\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "resource \"aws_security_group\" \"web\" {\n  name = \"web-sg-${var.version}\"\n\n  lifecycle {\n    create_before_destroy = true\n  }\n}"
    }
  ],
  keyTakeaway: "Use create_before_destroy for resources where replacement downtime is unacceptable.",
  references: ["https://developer.hashicorp.com/terraform/language/meta-arguments/lifecycle#create_before_destroy"]
},

"terraform-hcl.resource-semantics-lifecycle.is-ignore-changes-used-narrowly-and-documented-to-avoid-config-drift-bli": {
  whatItMeans: "ignore_changes should be used sparingly, targeting only specific attributes, and always documented to avoid creating drift blind spots.",
  whyItMatters: "ignore_changes tells Terraform to ignore differences between code and reality. Overuse creates invisible drift that surprises you later.",
  howToVerify: "Search for ignore_changes. Is it targeting specific attributes or all? Is there a comment explaining why?",
  exampleComment: "This uses ignore_changes = all which means Terraform will never detect any drift. Please narrow to specific attributes and document why.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "resource \"aws_ecs_service\" \"app\" {\n  desired_count = 3\n  lifecycle {\n    ignore_changes = all  # Ignores everything\n  }\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "resource \"aws_ecs_service\" \"app\" {\n  desired_count = 3\n  lifecycle {\n    # Autoscaling adjusts desired_count externally (INFRA-1234)\n    ignore_changes = [desired_count]\n  }\n}"
    }
  ],
  keyTakeaway: "ignore_changes is a scalpel, not a sledgehammer. Be specific and document the reason.",
  references: ["https://developer.hashicorp.com/terraform/language/meta-arguments/lifecycle#ignore_changes"]
},

"terraform-hcl.resource-semantics-lifecycle.are-explicit-depends-on-only-used-when-implicit-graphing-is-insufficient": {
  whatItMeans: "depends_on should only be used when Terraform cannot detect the dependency from resource references.",
  whyItMatters: "Unnecessary depends_on creates artificial ordering that slows applies and complicates the dependency graph.",
  howToVerify: "Check depends_on usage. Is the dependency already captured by a resource reference?",
  exampleComment: "This depends_on is unnecessary because the VPC ID reference already creates an implicit dependency.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "resource \"aws_subnet\" \"main\" {\n  vpc_id     = aws_vpc.main.id\n  depends_on = [aws_vpc.main]  # Redundant\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "resource \"aws_subnet\" \"main\" {\n  vpc_id = aws_vpc.main.id  # Implicit dependency sufficient\n}"
    }
  ],
  keyTakeaway: "Trust implicit dependency detection. Use depends_on only for hidden dependencies.",
  references: []
},

"terraform-hcl.resource-semantics-lifecycle.are-resources-imported-moved-using-import-moved-blocks-instead-of-ad-hoc": {
  whatItMeans: "Use Terraform 1.5+ import and moved blocks instead of ad hoc terraform state commands for state operations.",
  whyItMatters: "Import and moved blocks are declarative, reviewable, and repeatable. Ad hoc state surgery is error-prone and unauditable.",
  howToVerify: "Check for instructions to run terraform state commands. Can they be converted to import/moved blocks?",
  exampleComment: "The PR says to run terraform state mv manually. Please use a moved block instead.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "# Run: terraform state mv aws_instance.old aws_instance.new"
    },
    {
      label: "Good",
      language: "hcl",
      code: "moved {\n  from = aws_instance.old\n  to   = aws_instance.new\n}\n\nimport {\n  to = aws_s3_bucket.existing\n  id = \"my-existing-bucket\"\n}"
    }
  ],
  keyTakeaway: "Use moved and import blocks. Avoid ad hoc state surgery.",
  references: ["https://developer.hashicorp.com/terraform/language/import"]
},

"terraform-hcl.resource-semantics-lifecycle.are-rename-refactor-operations-planned-to-avoid-force-recreate-surprises": {
  whatItMeans: "Renaming resources without a moved block causes destroy and recreate, which is catastrophic for stateful resources.",
  whyItMatters: "A simple rename like changing aws_rds_instance.db to aws_rds_instance.database will destroy and recreate the database.",
  howToVerify: "Check the plan for destroy+create pairs that look like renames. Is a moved block in place?",
  exampleComment: "Renaming this RDS resource will destroy and recreate it. Please add a moved block.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "# Renamed from aws_rds_instance.db - causes destroy+create\nresource \"aws_rds_instance\" \"database\" {\n  identifier = \"my-db\"\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "moved {\n  from = aws_rds_instance.db\n  to   = aws_rds_instance.database\n}\n\nresource \"aws_rds_instance\" \"database\" {\n  identifier = \"my-db\"\n}"
    }
  ],
  keyTakeaway: "Always use moved blocks when renaming. A rename without moved is a destroy-and-recreate.",
  references: ["https://developer.hashicorp.com/terraform/language/modules/develop/refactoring"]
},

"terraform-hcl.resource-semantics-lifecycle.are-replacement-triggering-attribute-changes-clearly-called-out-in-revie": {
  whatItMeans: "Changes to attributes that trigger replacement (forces new) should be highlighted since they cause destruction and recreation.",
  whyItMatters: "Some attribute changes force resource replacement, causing downtime, data loss, and cascading replacements.",
  howToVerify: "Check the plan for 'forces replacement' annotations. Are these highlighted in the PR description?",
  exampleComment: "Changing availability_zone forces RDS replacement, which destroys the database. Please confirm backups exist.",
  codeExamples: [],
  keyTakeaway: "Replacement-triggering changes deserve the same scrutiny as explicit destroys.",
  references: []
},

"terraform-hcl.resource-semantics-lifecycle.are-timeout-blocks-configured-for-slow-provisioning-resources": {
  whatItMeans: "Resources that take a long time to provision should have custom timeout blocks to prevent false failures.",
  whyItMatters: "Default timeouts may be too short. A timeout failure mid-provisioning leaves resources in an unknown state.",
  howToVerify: "Check slow resources (RDS, EKS, CloudFront). Do they have timeout blocks?",
  exampleComment: "EKS creation can take 20+ minutes. Please add a timeout block with create = 30m.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "resource \"aws_eks_cluster\" \"main\" {\n  name     = \"main\"\n  role_arn = aws_iam_role.eks.arn\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "resource \"aws_eks_cluster\" \"main\" {\n  name     = \"main\"\n  role_arn = aws_iam_role.eks.arn\n\n  timeouts {\n    create = \"30m\"\n    update = \"30m\"\n    delete = \"20m\"\n  }\n}"
    }
  ],
  keyTakeaway: "Set timeouts for slow resources. False timeouts are worse than waiting.",
  references: []
},

"terraform-hcl.resource-semantics-lifecycle.are-eventual-consistency-issues-handled-with-robust-dependency-modeling": {
  whatItMeans: "Cloud APIs have eventual consistency where newly created resources may not be immediately visible. Dependencies should account for propagation delays.",
  whyItMatters: "IAM policies and DNS records may not be ready when dependent resources try to use them, causing intermittent apply failures.",
  howToVerify: "Check for known eventual consistency patterns (IAM, DNS). Are dependencies modeled to handle delays?",
  exampleComment: "The Lambda depends on the IAM role but IAM is eventually consistent. Consider adding depends_on for the role policy attachment.",
  codeExamples: [],
  keyTakeaway: "Cloud APIs are eventually consistent. Model dependencies to account for propagation delays.",
  references: []
},

"terraform-hcl.resource-semantics-lifecycle.are-data-resource-cycles-avoided-no-graph-deadlocks": {
  whatItMeans: "Data sources should not create circular dependencies with resources, as this causes Terraform graph deadlocks.",
  whyItMatters: "Circular dependencies prevent Terraform from determining execution order, causing plan failures.",
  howToVerify: "Check for data sources that reference resources that depend on the data source. Does terraform validate pass?",
  exampleComment: "This data source reads the security group that depends on this data source, creating a cycle. Break it with a variable.",
  codeExamples: [],
  keyTakeaway: "Data sources and resources must not form cycles. Break them with variables.",
  references: []
},

"terraform-hcl.resource-semantics-lifecycle.are-computed-values-not-used-in-ways-that-cause-perpetual-diffs": {
  whatItMeans: "Computed values that change on each apply should not be used in attributes that Terraform compares, as this causes perpetual plan diffs.",
  whyItMatters: "Perpetual diffs erode trust in plan output and make real changes hard to spot.",
  howToVerify: "Run terraform plan twice without changes. Are there any diffs? Check for timestamps or dynamic values.",
  exampleComment: "The tags include timestamp() which changes every plan. Please use a static value or ignore_changes.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "tags = {\n  LastUpdated = timestamp()  # Changes every plan\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "tags = {\n  LastUpdated = \"2024-03-15\"  # Static, updated manually\n}"
    }
  ],
  keyTakeaway: "Eliminate perpetual diffs. They destroy trust in terraform plan.",
  references: []
},

"terraform-hcl.resource-semantics-lifecycle.are-null-resource-and-provisioners-avoided-unless-truly-unavoidable": {
  whatItMeans: "null_resource and provisioners should be avoided in favor of native Terraform resources.",
  whyItMatters: "Provisioners are not tracked in state, not idempotent by default, and break the declarative model.",
  howToVerify: "Search for null_resource, local-exec, remote-exec. Is there a native resource alternative?",
  exampleComment: "This local-exec runs a shell script to configure the database. Could aws_db_parameter_group work instead?",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "resource \"null_resource\" \"config\" {\n  provisioner \"local-exec\" {\n    command = \"./configure-db.sh ${aws_rds_instance.main.endpoint}\"\n  }\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "resource \"aws_db_parameter_group\" \"main\" {\n  family = \"postgres14\"\n  parameter {\n    name  = \"max_connections\"\n    value = \"200\"\n  }\n}"
    }
  ],
  keyTakeaway: "Provisioners are an escape hatch, not a pattern. Use native resources.",
  references: ["https://developer.hashicorp.com/terraform/language/resources/provisioners/syntax"]
},

"terraform-hcl.resource-semantics-lifecycle.if-provisioners-are-used-are-they-idempotent-timeout-bounded-and-failure": {
  whatItMeans: "When provisioners are unavoidable, they must be idempotent, have timeouts, and handle failures gracefully.",
  whyItMatters: "Non-idempotent provisioners corrupt state on re-run. Without timeouts, a hung provisioner blocks the entire apply.",
  howToVerify: "Check provisioner scripts for idempotency. Is there a timeout? What happens on failure?",
  exampleComment: "This provisioner is not idempotent - running it twice creates duplicate entries. Please fix and add a timeout.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "provisioner \"local-exec\" {\n  command = \"./setup.sh\"  # No timeout, not idempotent\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "provisioner \"local-exec\" {\n  command    = \"timeout 300 ./setup.sh --idempotent\"\n  on_failure = fail\n}"
    }
  ],
  keyTakeaway: "If you must use provisioners, make them idempotent, timeboxed, and failure-aware.",
  references: []
},

"terraform-hcl.resource-semantics-lifecycle.are-external-scripts-pinned-versioned-and-reviewable-when-called-from-te": {
  whatItMeans: "External scripts called from Terraform should be version-controlled, reviewed, and pinned to specific versions.",
  whyItMatters: "An unversioned script can change behavior without a Terraform code change, breaking reproducibility.",
  howToVerify: "Check references to external scripts. Are they in the repository? Are they versioned?",
  exampleComment: "This local-exec calls a script from a URL. Please vendor it into the repo or pin to a commit.",
  codeExamples: [],
  keyTakeaway: "External scripts must be versioned and reviewed. If it affects infrastructure, it must be in VCS.",
  references: []
},

"terraform-hcl.resource-semantics-lifecycle.is-graph-fan-out-manageable-to-avoid-provider-api-throttling-during-appl": {
  whatItMeans: "The resource graph should not fan out too widely to avoid overwhelming provider APIs with parallel requests.",
  whyItMatters: "Provider APIs have rate limits. Creating too many resources simultaneously triggers throttling and failures.",
  howToVerify: "Check if the plan creates many resources of the same type simultaneously. Should parallelism be reduced?",
  exampleComment: "This plan creates 50 Lambda functions simultaneously, which will hit API throttling. Consider -parallelism=5.",
  codeExamples: [],
  keyTakeaway: "Be mindful of API rate limits. Reduce parallelism for large deployments.",
  references: []
},

"terraform-hcl.resource-semantics-lifecycle.are-large-plans-split-logically-to-keep-review-and-apply-risk-bounded": {
  whatItMeans: "Large plans with many changes should be split into smaller, logical units to keep review quality high and blast radius bounded.",
  whyItMatters: "A plan changing 200 resources is impossible to review thoroughly and has a massive blast radius.",
  howToVerify: "Check the plan size. If it changes 20+ resources, could it be split into phases?",
  exampleComment: "This plan modifies 150 resources. Consider splitting into networking, compute, and database phases.",
  codeExamples: [],
  keyTakeaway: "Large plans are high-risk. Split into smaller, independently appliable units.",
  references: []
},

"terraform-hcl.resource-semantics-lifecycle.are-taint-style-workflows-avoided-in-favor-of-declarative-reviewable-cod": {
  whatItMeans: "terraform taint should be avoided in favor of declarative code changes that can be reviewed in PRs.",
  whyItMatters: "Tainting is imperative, bypasses review, and is not tracked in code.",
  howToVerify: "Check for taint instructions. Can the recreation be achieved with a code change instead?",
  exampleComment: "Instead of tainting, consider adding a triggers value to force recreation declaratively.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "# Run: terraform taint aws_instance.web"
    },
    {
      label: "Good",
      language: "hcl",
      code: "resource \"aws_instance\" \"web\" {\n  ami = var.ami_id\n  tags = {\n    ForceRecreate = \"v2\"  # Bump to trigger replace\n  }\n}"
    }
  ],
  keyTakeaway: "Make resource recreation declarative. Taint is an escape hatch, not a workflow.",
  references: []
},

"terraform-hcl.resource-semantics-lifecycle.are-drift-prone-resources-monitored-and-reconciled-intentionally": {
  whatItMeans: "Resources frequently modified outside Terraform should be monitored for drift and reconciled intentionally.",
  whyItMatters: "Drift between code and reality creates a false sense of what is deployed.",
  howToVerify: "Identify drift-prone resources. Is drift detection configured? Is there a reconciliation process?",
  exampleComment: "Security group rules are modified manually. Set up drift detection or add ignore_changes with documentation.",
  codeExamples: [],
  keyTakeaway: "Monitor for drift. What you do not know about your infrastructure can hurt you.",
  references: []
},

"terraform-hcl.resource-semantics-lifecycle.are-resource-names-immutable-stable-where-replacement-is-costly": {
  whatItMeans: "Resource names that trigger replacement when changed should be stable and not derived from volatile values.",
  whyItMatters: "Changing immutable names forces replacement, which means data loss for databases and storage.",
  howToVerify: "Check resources with immutable names. Are names derived from stable inputs?",
  exampleComment: "The RDS identifier includes a variable that could change. Use a stable name and add prevent_destroy.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "resource \"aws_rds_instance\" \"main\" {\n  identifier = \"${var.project}-${var.env_name}-db\"\n  # env_name change = database destroyed\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "resource \"aws_rds_instance\" \"main\" {\n  identifier = \"myapp-prod-db\"\n  lifecycle { prevent_destroy = true }\n}"
    }
  ],
  keyTakeaway: "Immutable names should be stable. Do not derive them from values that might change.",
  references: []
},

"terraform-hcl.resource-semantics-lifecycle.are-cross-region-cross-account-dependencies-explicit-and-resilient": {
  whatItMeans: "Dependencies across regions or accounts should be explicit, documented, and resilient to temporary failures.",
  whyItMatters: "Cross-region dependencies introduce failure modes from network partitions and regional outages.",
  howToVerify: "Check for data sources or references that reach across regions or accounts. Are dependencies documented?",
  exampleComment: "This reads from another AWS account. If permissions change, this plan fails. Please document the dependency.",
  codeExamples: [],
  keyTakeaway: "Cross-region and cross-account dependencies are fragile. Document them and plan for failures.",
  references: []
},

"terraform-hcl.resource-semantics-lifecycle.are-default-provider-region-assumptions-avoided-in-resource-blocks": {
  whatItMeans: "Resources with region-specific requirements should explicitly reference the correct provider alias.",
  whyItMatters: "Resources without explicit providers inherit the default, whose region might change unexpectedly.",
  howToVerify: "Check region-specific resources. Do they reference the correct provider alias?",
  exampleComment: "This ACM certificate for CloudFront must be in us-east-1 but uses the default provider. Add a provider reference.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "resource \"aws_acm_certificate\" \"cdn\" {\n  domain_name = \"example.com\"\n  # Uses default provider region\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "resource \"aws_acm_certificate\" \"cdn\" {\n  provider    = aws.us_east_1\n  domain_name = \"example.com\"\n}"
    }
  ],
  keyTakeaway: "Be explicit about provider regions for region-specific resources.",
  references: []
},

"terraform-hcl.resource-semantics-lifecycle.are-cloud-api-eventual-consistency-retries-backoff-considered-in-provide": {
  whatItMeans: "Provider retry settings should account for cloud API eventual consistency, especially for high-throughput operations.",
  whyItMatters: "Without retries, transient API failures require manual re-runs of terraform apply.",
  howToVerify: "Check provider retry configuration. Are there known transient failure patterns?",
  exampleComment: "This creates many IAM resources which are eventually consistent. Consider configuring retry_mode = standard.",
  codeExamples: [],
  keyTakeaway: "Configure provider retries for eventual consistency to prevent unnecessary manual re-runs.",
  references: []
},

"terraform-hcl.resource-semantics-lifecycle.is-rollback-behavior-understood-for-partial-failures-in-apply": {
  whatItMeans: "The team should understand what happens when apply fails partway: which resources exist, which do not, and how to recover.",
  whyItMatters: "Terraform does not roll back on failure. Partial applies leave infrastructure in potentially inconsistent states.",
  howToVerify: "Consider what happens if apply fails after some resources are created. Is the intermediate state functional?",
  exampleComment: "If this fails after creating the VPC but before the subnets, infrastructure will be broken. Add a recovery plan.",
  codeExamples: [],
  keyTakeaway: "Terraform does not roll back on failure. Plan for partial apply states.",
  references: []
},

// ============================================================
// SECURITY, IAM, SECRETS & NETWORK (items 125-154)
// ============================================================

"terraform-hcl.security-iam-secrets-network.are-iam-roles-policies-least-privilege-and-scoped-to-explicit-actions-re": {
  whatItMeans: "IAM roles and policies should grant only the minimum permissions needed, with explicit actions and resource ARNs rather than wildcards.",
  whyItMatters: "Overly permissive IAM policies are the most common cloud security vulnerability. A compromised service with broad permissions can access or destroy any resource in the account.",
  howToVerify: "Review IAM policy documents. Are actions specific (s3:GetObject) or wildcarded (s3:*)? Are resources scoped to specific ARNs or using *?",
  exampleComment: "This IAM policy grants s3:* on all resources. Please scope to the specific actions and bucket ARN needed.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "resource \"aws_iam_role_policy\" \"app\" {\n  role   = aws_iam_role.app.id\n  policy = jsonencode({\n    Version = \"2012-10-17\"\n    Statement = [{\n      Effect   = \"Allow\"\n      Action   = \"s3:*\"\n      Resource = \"*\"\n    }]\n  })\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "resource \"aws_iam_role_policy\" \"app\" {\n  role   = aws_iam_role.app.id\n  policy = jsonencode({\n    Version = \"2012-10-17\"\n    Statement = [{\n      Effect   = \"Allow\"\n      Action   = [\"s3:GetObject\", \"s3:PutObject\"]\n      Resource = \"${aws_s3_bucket.data.arn}/*\"\n    }]\n  })\n}"
    }
  ],
  keyTakeaway: "Least privilege is non-negotiable. Scope every action and resource explicitly.",
  references: ["https://docs.aws.amazon.com/IAM/latest/UserGuide/best-practices.html#grant-least-privilege"]
},

"terraform-hcl.security-iam-secrets-network.are-wildcard-iam-grants-avoided-unless-strongly-justified-and-documented": {
  whatItMeans: "Wildcard (*) grants in IAM policies should be avoided. When they are truly necessary, they should be documented with a justification.",
  whyItMatters: "A wildcard on actions means the role can do anything with that service. A wildcard on resources means it applies to everything. Both dramatically increase the blast radius of a compromise.",
  howToVerify: "Search IAM policy documents for Action = \"*\" or Resource = \"*\". Are they justified? Can they be narrowed?",
  exampleComment: "This policy uses Action = '*' which grants full admin access. This is almost never appropriate. Please narrow to specific actions.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "Statement = [{\n  Effect   = \"Allow\"\n  Action   = \"*\"\n  Resource = \"*\"\n}]"
    },
    {
      label: "Good",
      language: "hcl",
      code: "Statement = [{\n  Effect   = \"Allow\"\n  Action   = [\n    \"logs:CreateLogGroup\",\n    \"logs:CreateLogStream\",\n    \"logs:PutLogEvents\"\n  ]\n  Resource = \"arn:aws:logs:*:*:log-group:/app/*\"\n}]"
    }
  ],
  keyTakeaway: "Every wildcard is a risk. Justify each one and document why it cannot be narrowed.",
  references: []
},

"terraform-hcl.security-iam-secrets-network.are-trust-policies-constrained-principal-conditions-audience-external-id": {
  whatItMeans: "IAM trust policies (assume role policies) should restrict which principals can assume the role, with additional conditions like external ID, audience, or source account.",
  whyItMatters: "An overly permissive trust policy allows unintended principals to assume the role. Conditions like external ID prevent confused deputy attacks in cross-account scenarios.",
  howToVerify: "Review assume_role_policy documents. Is the Principal specific? Are conditions used for cross-account or OIDC scenarios?",
  exampleComment: "This cross-account trust policy has no external ID condition. Please add one to prevent confused deputy attacks.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "assume_role_policy = jsonencode({\n  Statement = [{\n    Effect    = \"Allow\"\n    Principal = { AWS = \"*\" }  # Anyone can assume\n    Action    = \"sts:AssumeRole\"\n  }]\n})"
    },
    {
      label: "Good",
      language: "hcl",
      code: "assume_role_policy = jsonencode({\n  Statement = [{\n    Effect    = \"Allow\"\n    Principal = { AWS = \"arn:aws:iam::123456789012:root\" }\n    Action    = \"sts:AssumeRole\"\n    Condition = {\n      StringEquals = {\n        \"sts:ExternalId\" = var.external_id\n      }\n    }\n  }]\n})"
    }
  ],
  keyTakeaway: "Trust policies need constraints. Principal + conditions prevent unauthorized role assumption.",
  references: []
},

"terraform-hcl.security-iam-secrets-network.are-public-ingress-egress-paths-intentional-and-tightly-restricted": {
  whatItMeans: "Any public ingress (internet-facing) or egress paths should be intentional, documented, and restricted to the minimum necessary ports and CIDRs.",
  whyItMatters: "Unintended public exposure is a leading cause of cloud security breaches. Every public path should be a deliberate decision.",
  howToVerify: "Check for security group rules, NACLs, and load balancers with public access. Are they intentional? Are they restricted to necessary ports?",
  exampleComment: "This security group allows ingress on all ports from 0.0.0.0/0. Is public access intended? If so, please restrict to specific ports.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "resource \"aws_security_group_rule\" \"all_ingress\" {\n  type        = \"ingress\"\n  from_port   = 0\n  to_port     = 65535\n  protocol    = \"tcp\"\n  cidr_blocks = [\"0.0.0.0/0\"]\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "resource \"aws_security_group_rule\" \"https_ingress\" {\n  type        = \"ingress\"\n  from_port   = 443\n  to_port     = 443\n  protocol    = \"tcp\"\n  cidr_blocks = [\"0.0.0.0/0\"]\n  description = \"HTTPS from internet - public ALB\"\n}"
    }
  ],
  keyTakeaway: "Every public path should be intentional, documented, and minimally scoped.",
  references: []
},

"terraform-hcl.security-iam-secrets-network.are-security-groups-firewall-rules-avoiding-broad-cidrs-0-0-0-0-0-where": {
  whatItMeans: "Security groups and firewall rules should use specific CIDR ranges rather than 0.0.0.0/0 unless public internet access is genuinely required.",
  whyItMatters: "0.0.0.0/0 opens the resource to the entire internet. Most resources should only be accessible from specific networks, VPCs, or IP ranges.",
  howToVerify: "Search for 0.0.0.0/0 in security group rules. Is public access required for each occurrence? Can it be narrowed to a VPC CIDR or specific IP range?",
  exampleComment: "This database security group allows ingress from 0.0.0.0/0. Databases should never be publicly accessible. Please restrict to the application subnet CIDR.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "resource \"aws_security_group_rule\" \"db\" {\n  type        = \"ingress\"\n  from_port   = 5432\n  to_port     = 5432\n  protocol    = \"tcp\"\n  cidr_blocks = [\"0.0.0.0/0\"]  # DB open to internet\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "resource \"aws_security_group_rule\" \"db\" {\n  type                     = \"ingress\"\n  from_port                = 5432\n  to_port                  = 5432\n  protocol                 = \"tcp\"\n  source_security_group_id = aws_security_group.app.id\n  description              = \"PostgreSQL from app tier only\"\n}"
    }
  ],
  keyTakeaway: "0.0.0.0/0 should be rare and intentional. Most resources need access from specific sources only.",
  references: []
},

"terraform-hcl.security-iam-secrets-network.are-data-stores-and-object-storage-encrypted-at-rest-with-managed-custom": {
  whatItMeans: "All data stores (databases, caches) and object storage should have encryption at rest enabled with either AWS-managed or customer-managed keys.",
  whyItMatters: "Encryption at rest protects data if the underlying storage media is compromised. Many compliance frameworks require it.",
  howToVerify: "Check database, S3, and other storage resources for encryption settings. Is encryption enabled? Is the key type appropriate for the data sensitivity?",
  exampleComment: "This RDS instance does not have storage_encrypted = true. Please enable encryption at rest.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "resource \"aws_rds_instance\" \"main\" {\n  identifier     = \"app-db\"\n  engine         = \"postgres\"\n  instance_class = \"db.r5.large\"\n  # No encryption\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "resource \"aws_rds_instance\" \"main\" {\n  identifier        = \"app-db\"\n  engine            = \"postgres\"\n  instance_class    = \"db.r5.large\"\n  storage_encrypted = true\n  kms_key_id        = aws_kms_key.db.arn\n}"
    }
  ],
  keyTakeaway: "Encrypt everything at rest. It is a baseline security requirement.",
  references: []
},

"terraform-hcl.security-iam-secrets-network.is-encryption-in-transit-enforced-tls-settings-https-listeners-secure-pr": {
  whatItMeans: "Encryption in transit should be enforced via TLS settings, HTTPS listeners, and secure protocol configurations.",
  whyItMatters: "Unencrypted traffic can be intercepted and read. HTTPS and TLS are baseline requirements for any production service.",
  howToVerify: "Check load balancer listeners, API gateways, and service configurations. Is TLS enforced? Is the minimum TLS version appropriate?",
  exampleComment: "This ALB listener uses HTTP on port 80 without redirecting to HTTPS. Please add TLS or an HTTP-to-HTTPS redirect.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "resource \"aws_lb_listener\" \"app\" {\n  load_balancer_arn = aws_lb.app.arn\n  port              = 80\n  protocol          = \"HTTP\"  # Unencrypted\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "resource \"aws_lb_listener\" \"app\" {\n  load_balancer_arn = aws_lb.app.arn\n  port              = 443\n  protocol          = \"HTTPS\"\n  ssl_policy        = \"ELBSecurityPolicy-TLS13-1-2-2021-06\"\n  certificate_arn   = aws_acm_certificate.app.arn\n}"
    }
  ],
  keyTakeaway: "Enforce TLS everywhere. Unencrypted traffic is unacceptable in production.",
  references: []
},

"terraform-hcl.security-iam-secrets-network.are-secrets-retrieved-from-a-secret-manager-kms-flow-not-plaintext-varia": {
  whatItMeans: "Secrets should be retrieved from a secrets manager (AWS Secrets Manager, Vault, etc.) rather than stored as plaintext in variables, locals, or tfvars.",
  whyItMatters: "Plaintext secrets in Terraform code end up in state files, plan output, and CI logs. A secrets manager provides rotation, access control, and audit logging.",
  howToVerify: "Search for password, secret, key, or token in variable defaults and locals. Are they referencing a secrets manager or hardcoded?",
  exampleComment: "The database password is a plaintext variable. Please retrieve it from AWS Secrets Manager instead.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "variable \"db_password\" {\n  type    = string\n  default = \"MyS3cretP@ss!\"  # Plaintext in code\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "data \"aws_secretsmanager_secret_version\" \"db\" {\n  secret_id = \"app/database/password\"\n}\n\nresource \"aws_rds_instance\" \"main\" {\n  password = data.aws_secretsmanager_secret_version.db.secret_string\n}"
    }
  ],
  keyTakeaway: "Never store secrets in Terraform code or variables. Use a secrets manager.",
  references: []
},

"terraform-hcl.security-iam-secrets-network.are-secret-values-prevented-from-leaking-into-outputs-logs-and-plan-comm": {
  whatItMeans: "Secret values should not appear in outputs, CI logs, or plan comments. Use sensitive = true and avoid logging patterns that expose secrets.",
  whyItMatters: "Secrets in plan output or CI logs are visible to anyone with pipeline access. Once exposed, credentials must be rotated immediately.",
  howToVerify: "Check outputs for sensitive values. Are they marked sensitive? Could plan output expose secrets in PR comments?",
  exampleComment: "The connection_string output contains the password in plaintext. Please mark it as sensitive.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "output \"connection_string\" {\n  value = \"postgres://admin:${var.password}@${aws_rds_instance.main.endpoint}/db\"\n  # Password visible in plan output\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "output \"connection_string\" {\n  value     = \"postgres://admin:${var.password}@${aws_rds_instance.main.endpoint}/db\"\n  sensitive = true\n}"
    }
  ],
  keyTakeaway: "Mark all secret-containing values as sensitive. Check plan output for leaks.",
  references: []
},

"terraform-hcl.security-iam-secrets-network.are-key-rotation-and-secret-rotation-patterns-supported-by-the-infrastru": {
  whatItMeans: "The infrastructure should support key and secret rotation without downtime or manual intervention.",
  whyItMatters: "Credentials that cannot be rotated become permanent vulnerabilities. Infrastructure should be designed so that rotation is seamless.",
  howToVerify: "Check how secrets and keys are consumed. Can they be rotated without redeploying resources? Is there a rotation mechanism in place?",
  exampleComment: "The database password is embedded in the ECS task definition. Rotating it requires a new deployment. Consider using Secrets Manager with dynamic retrieval.",
  codeExamples: [],
  keyTakeaway: "Design for rotation from the start. Secrets that cannot be rotated are ticking time bombs.",
  references: []
},

"terraform-hcl.security-iam-secrets-network.are-kms-key-policies-scoped-and-audited-including-admin-separation-where": {
  whatItMeans: "KMS key policies should follow least privilege with separate admin and usage permissions, and key usage should be auditable via CloudTrail.",
  whyItMatters: "An overly permissive KMS key policy allows unauthorized encryption/decryption. Admin separation prevents users who can use keys from also managing them.",
  howToVerify: "Review KMS key policies. Are admin and usage permissions separated? Is the key policy scoped to specific principals?",
  exampleComment: "This KMS key policy grants kms:* to the entire account. Please separate admin and usage permissions.",
  codeExamples: [],
  keyTakeaway: "KMS key policies need least privilege and admin separation. Audit key usage via CloudTrail.",
  references: []
},

"terraform-hcl.security-iam-secrets-network.are-bucket-storage-policies-denying-public-access-by-default": {
  whatItMeans: "S3 buckets and object storage should deny public access by default using bucket policies and public access blocks.",
  whyItMatters: "Publicly accessible storage is one of the most common sources of data breaches. Default-deny prevents accidental exposure.",
  howToVerify: "Check S3 buckets for public access block configuration. Is public access explicitly blocked?",
  exampleComment: "This S3 bucket does not have a public access block. Please add aws_s3_bucket_public_access_block to deny public access.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "resource \"aws_s3_bucket\" \"data\" {\n  bucket = \"my-data-bucket\"\n  # No public access block\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "resource \"aws_s3_bucket\" \"data\" {\n  bucket = \"my-data-bucket\"\n}\n\nresource \"aws_s3_bucket_public_access_block\" \"data\" {\n  bucket                  = aws_s3_bucket.data.id\n  block_public_acls       = true\n  block_public_policy     = true\n  ignore_public_acls      = true\n  restrict_public_buckets = true\n}"
    }
  ],
  keyTakeaway: "Block public access on all storage by default. Public access should be a deliberate exception.",
  references: []
},

"terraform-hcl.security-iam-secrets-network.are-network-acls-route-tables-and-subnet-boundaries-aligned-to-trust-zon": {
  whatItMeans: "Network ACLs, route tables, and subnets should align to trust zones (public, private, restricted) with appropriate routing between them.",
  whyItMatters: "Misaligned network boundaries can allow unintended traffic flows between trust zones, undermining the network security model.",
  howToVerify: "Review subnet design, route tables, and NACLs. Are public, private, and restricted subnets clearly separated? Do routes match the intended trust model?",
  exampleComment: "The private subnet has a route to the internet gateway. Private subnets should route through NAT for outbound only.",
  codeExamples: [],
  keyTakeaway: "Align network boundaries to trust zones. Public, private, and restricted subnets should have distinct routing.",
  references: []
},

"terraform-hcl.security-iam-secrets-network.are-private-endpoints-peering-transit-configs-avoiding-unintended-latera": {
  whatItMeans: "VPC peering, transit gateways, and private endpoints should be configured to prevent unintended lateral movement between networks.",
  whyItMatters: "Poorly configured peering or transit can allow traffic between networks that should be isolated, expanding the blast radius of a security incident.",
  howToVerify: "Review VPC peering, transit gateway routes, and private endpoint configurations. Are routes scoped? Could traffic flow between unintended networks?",
  exampleComment: "This VPC peering connection does not restrict routes. All traffic from the dev VPC can reach the prod VPC. Please add route table restrictions.",
  codeExamples: [],
  keyTakeaway: "Peering and transit configs need explicit route scoping. Prevent unintended lateral movement.",
  references: []
},

"terraform-hcl.security-iam-secrets-network.are-logging-monitoring-resources-secured-against-tampering-and-unauthori": {
  whatItMeans: "Logging and monitoring infrastructure (CloudTrail, CloudWatch, S3 log buckets) should be protected against tampering and unauthorized access.",
  whyItMatters: "If an attacker can delete or modify logs, they can cover their tracks. Immutable logging is essential for incident investigation.",
  howToVerify: "Check that log buckets have versioning, object lock, and restricted delete permissions. Is CloudTrail protected from disabling?",
  exampleComment: "The CloudTrail log bucket allows delete operations. Please enable object lock or restrict delete permissions to prevent log tampering.",
  codeExamples: [],
  keyTakeaway: "Protect logs from tampering. Immutable logging is essential for security investigations.",
  references: []
},

"terraform-hcl.security-iam-secrets-network.are-admin-interfaces-restricted-ip-allowlists-sso-mfa-break-glass-contro": {
  whatItMeans: "Administrative interfaces should be restricted with IP allowlists, SSO, MFA, and break-glass emergency access controls.",
  whyItMatters: "Unrestricted admin access is a critical vulnerability. Layered access controls (IP restriction + SSO + MFA) significantly reduce the attack surface.",
  howToVerify: "Check admin-facing resources for access restrictions. Are management ports (22, 3389) restricted? Is SSO/MFA enforced for admin access?",
  exampleComment: "The bastion host security group allows SSH from 0.0.0.0/0. Please restrict to the VPN CIDR or use SSM Session Manager instead.",
  codeExamples: [],
  keyTakeaway: "Restrict admin interfaces with multiple layers: IP allowlists, SSO, MFA, and break-glass procedures.",
  references: []
},

"terraform-hcl.security-iam-secrets-network.are-compliance-relevant-controls-represented-as-code-retention-encryptio": {
  whatItMeans: "Compliance requirements like data retention, encryption, and access logging should be codified in Terraform rather than relying on manual implementation.",
  whyItMatters: "Codified compliance controls are auditable, reproducible, and cannot be accidentally skipped. Manual compliance steps are error-prone and hard to verify.",
  howToVerify: "Check if compliance requirements (retention policies, encryption, access logs) are implemented in Terraform code. Are they enforced via policy-as-code?",
  exampleComment: "SOC2 requires 90-day log retention but this is not codified. Please add lifecycle rules to enforce retention.",
  codeExamples: [],
  keyTakeaway: "Codify compliance controls. Manual compliance is unreliable compliance.",
  references: []
},

"terraform-hcl.security-iam-secrets-network.are-service-accounts-identities-separated-by-workload-and-environment": {
  whatItMeans: "Service accounts and IAM roles should be separated per workload and per environment, not shared across multiple services or environments.",
  whyItMatters: "Shared service accounts expand the blast radius of a compromise. If one service is compromised, all services sharing its identity are affected.",
  howToVerify: "Check IAM roles and service accounts. Is each workload using its own identity? Are identities separated across environments?",
  exampleComment: "The same IAM role is used by both the web and worker services. Please create separate roles so compromising one does not affect the other.",
  codeExamples: [],
  keyTakeaway: "One identity per workload per environment. Shared identities are shared risk.",
  references: []
},

"terraform-hcl.security-iam-secrets-network.are-default-passwords-test-credentials-and-insecure-bootstrap-paths-abse": {
  whatItMeans: "The code should not contain default passwords, test credentials, or insecure bootstrap paths that could be exploited.",
  whyItMatters: "Default and test credentials are the first thing attackers try. They are often left in code after development and become production vulnerabilities.",
  howToVerify: "Search for common default passwords, 'admin', 'password', 'test', and hardcoded credential patterns in the code.",
  exampleComment: "The database has a default password of 'admin123'. Please use a randomly generated password from Secrets Manager.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "resource \"aws_rds_instance\" \"main\" {\n  username = \"admin\"\n  password = \"admin123\"  # Default credential\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "resource \"random_password\" \"db\" {\n  length  = 32\n  special = true\n}\n\nresource \"aws_rds_instance\" \"main\" {\n  username = var.db_username\n  password = random_password.db.result\n}"
    }
  ],
  keyTakeaway: "No default passwords, ever. Generate credentials randomly and manage them securely.",
  references: []
},

"terraform-hcl.security-iam-secrets-network.are-policy-conditions-used-for-time-ip-device-context-restrictions-where": {
  whatItMeans: "IAM policy conditions should be used to add contextual restrictions like IP ranges, time windows, MFA requirements, and source VPC where feasible.",
  whyItMatters: "Conditions add defense-in-depth beyond basic allow/deny. Even if credentials are stolen, conditions can limit where and when they can be used.",
  howToVerify: "Review IAM policies for Condition blocks. Are sensitive operations restricted by IP, MFA, or source VPC?",
  exampleComment: "This admin policy has no conditions. Consider adding an MFA condition and IP restriction for sensitive operations.",
  codeExamples: [],
  keyTakeaway: "Use policy conditions for defense-in-depth. They limit damage even when credentials are compromised.",
  references: []
},

"terraform-hcl.security-iam-secrets-network.are-denial-guardrails-present-for-destructive-high-risk-actions": {
  whatItMeans: "Explicit deny policies should be in place for destructive or high-risk actions that should never be performed, even by admins.",
  whyItMatters: "Deny policies override allow policies and provide guardrails that cannot be bypassed. They prevent catastrophic actions like deleting CloudTrail or disabling encryption.",
  howToVerify: "Check for deny policies on critical actions like deleting logging, disabling encryption, or removing guardrails.",
  exampleComment: "There is no deny policy preventing CloudTrail deletion. Please add a guardrail deny policy for critical security services.",
  codeExamples: [],
  keyTakeaway: "Deny policies are guardrails. They prevent catastrophic actions even from privileged users.",
  references: []
},

"terraform-hcl.security-iam-secrets-network.are-sensitive-tags-labels-avoided-if-they-expose-internal-security-conte": {
  whatItMeans: "Tags and labels should not expose internal security details like vulnerability status, compliance gaps, or security tool names.",
  whyItMatters: "Tags are often visible to broader audiences than intended. A tag like 'unpatched: true' or 'waf: disabled' provides an attacker with targeting information.",
  howToVerify: "Review tags for information that could help an attacker. Do they expose security posture, vulnerability status, or internal tool names?",
  exampleComment: "The tag 'SecurityStatus: non-compliant' exposes internal security posture. Please remove or generalize security-related tags.",
  codeExamples: [],
  keyTakeaway: "Tags are visible. Do not tag resources with information that helps attackers.",
  references: []
},

"terraform-hcl.security-iam-secrets-network.are-dns-public-endpoint-exposures-intentional-and-reviewed-with-owning-t": {
  whatItMeans: "DNS records pointing to public endpoints should be intentional, reviewed, and approved by the team that owns the service.",
  whyItMatters: "A misconfigured DNS record can expose internal services to the internet or point users to the wrong endpoint.",
  howToVerify: "Check Route53 or DNS resources. Are new public records intentional? Have they been reviewed by the service owner?",
  exampleComment: "This DNS record exposes an internal API to the public internet. Is this intentional? Please confirm with the API team.",
  codeExamples: [],
  keyTakeaway: "Public DNS records should be deliberate. Every public endpoint is an attack surface.",
  references: []
},

"terraform-hcl.security-iam-secrets-network.are-tls-cert-lifecycles-automated-and-monitored-for-expiry": {
  whatItMeans: "TLS certificate provisioning, renewal, and expiry monitoring should be automated to prevent outages from expired certificates.",
  whyItMatters: "Expired TLS certificates cause immediate service outages. Manual certificate management is error-prone and does not scale.",
  howToVerify: "Check how TLS certificates are provisioned. Are they using ACM or Let's Encrypt with auto-renewal? Is there monitoring for approaching expiry?",
  exampleComment: "This certificate is manually provisioned. Consider using ACM with DNS validation for automatic renewal.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "resource \"aws_iam_server_certificate\" \"app\" {\n  name             = \"app-cert\"\n  certificate_body = file(\"certs/app.crt\")  # Manual cert\n  private_key      = file(\"certs/app.key\")\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "resource \"aws_acm_certificate\" \"app\" {\n  domain_name       = \"app.example.com\"\n  validation_method = \"DNS\"\n\n  lifecycle {\n    create_before_destroy = true\n  }\n}"
    }
  ],
  keyTakeaway: "Automate TLS certificate lifecycle. Manual certificate management causes outages.",
  references: []
},

"terraform-hcl.security-iam-secrets-network.are-backup-snapshots-encrypted-and-access-controlled": {
  whatItMeans: "Database snapshots, EBS snapshots, and other backups should be encrypted and have access controls to prevent unauthorized access.",
  whyItMatters: "Unencrypted backups are a common data exfiltration vector. If someone gains access to an unencrypted snapshot, they can read all the data.",
  howToVerify: "Check snapshot and backup configurations. Are they encrypted? Are sharing permissions restricted?",
  exampleComment: "This RDS snapshot is not encrypted because the source database is not encrypted. Please enable encryption on the database.",
  codeExamples: [],
  keyTakeaway: "Encrypt backups and restrict access. Unencrypted snapshots are data breaches waiting to happen.",
  references: []
},

"terraform-hcl.security-iam-secrets-network.are-infrastructure-logs-routed-to-centralized-immutable-storage-where-re": {
  whatItMeans: "Infrastructure logs should be routed to centralized, immutable storage for security analysis and compliance.",
  whyItMatters: "Centralized logging enables correlation across services and prevents attackers from covering tracks by deleting local logs.",
  howToVerify: "Check logging configurations. Are logs sent to a central location? Is the storage immutable (object lock, write-once)?",
  exampleComment: "VPC flow logs are not configured. Please enable them and route to the central log bucket.",
  codeExamples: [],
  keyTakeaway: "Centralize and protect logs. Scattered, mutable logs are useless for incident response.",
  references: []
},

"terraform-hcl.security-iam-secrets-network.are-cloud-native-threat-detection-integrations-enabled-where-expected": {
  whatItMeans: "Cloud-native threat detection services (GuardDuty, Security Hub, Cloud Armor) should be enabled and integrated where the organization expects them.",
  whyItMatters: "Threat detection services provide automated monitoring for suspicious activity. Without them, security incidents may go undetected for extended periods.",
  howToVerify: "Check if threat detection services are enabled. Are findings routed to the security team? Are new accounts automatically enrolled?",
  exampleComment: "GuardDuty is not enabled in this account. Please enable it per organizational security requirements.",
  codeExamples: [],
  keyTakeaway: "Enable cloud-native threat detection. It is your automated security watchdog.",
  references: []
},

"terraform-hcl.security-iam-secrets-network.are-ephemeral-preview-environments-constrained-to-prevent-data-exfiltrat": {
  whatItMeans: "Ephemeral or preview environments should be constrained with limited permissions and network access to prevent data exfiltration.",
  whyItMatters: "Preview environments often have weaker security controls but may have access to production data or APIs. Without constraints, they can be used for data exfiltration.",
  howToVerify: "Check ephemeral environment configurations. Are they network-isolated? Do they have access to production data? Are permissions minimized?",
  exampleComment: "This preview environment has the same IAM permissions as production. Please create a restricted role for preview environments.",
  codeExamples: [],
  keyTakeaway: "Constrain ephemeral environments. They should have minimal access and network isolation.",
  references: []
},

"terraform-hcl.security-iam-secrets-network.are-service-to-service-auth-patterns-explicit-mtls-iam-auth-workload-ide": {
  whatItMeans: "Service-to-service authentication should use explicit mechanisms like mTLS, IAM authentication, or workload identity rather than shared secrets or network-level trust.",
  whyItMatters: "Network-level trust (assuming services on the same network are authorized) is insufficient. Explicit authentication prevents unauthorized service communication.",
  howToVerify: "Check how services authenticate to each other. Is it network-based or identity-based? Are there explicit auth mechanisms?",
  exampleComment: "The services communicate over plain HTTP with no authentication. Consider adding IAM auth or mTLS for service-to-service communication.",
  codeExamples: [],
  keyTakeaway: "Authenticate services explicitly. Network proximity is not authentication.",
  references: []
},

"terraform-hcl.security-iam-secrets-network.are-third-party-integrations-scoped-and-reviewed-for-least-privilege": {
  whatItMeans: "Third-party integrations (SaaS tools, CI/CD, monitoring) should have scoped permissions and be reviewed for least privilege.",
  whyItMatters: "Third-party integrations are a common attack vector. Overly permissive third-party access extends the trust boundary beyond the organization.",
  howToVerify: "Check IAM roles and policies for third-party integrations. Are they scoped to minimum needed permissions? Are external IDs and conditions used?",
  exampleComment: "This third-party monitoring tool has ReadOnlyAccess to the entire account. Please scope to the specific resources it needs to monitor.",
  codeExamples: [],
  keyTakeaway: "Third-party integrations need scoped, least-privilege access. Trust but verify.",
  references: []
},

// ============================================================
// RELIABILITY, OBSERVABILITY & DR (items 155-172)
// ============================================================

"terraform-hcl.reliability-observability-dr.are-ha-expectations-encoded-multi-az-zone-regional-redundancy-for-critic": {
  whatItMeans: "High availability requirements should be encoded in the infrastructure: multi-AZ deployments, regional redundancy, and replicas for critical services.",
  whyItMatters: "A single-AZ database or compute instance is a single point of failure. When that AZ has an outage, the service goes down entirely.",
  howToVerify: "Check critical resources for multi-AZ configuration. Is the RDS instance multi-AZ? Are instances spread across AZs? Is the architecture resilient to single-AZ failure?",
  exampleComment: "This production RDS instance is single-AZ. An AZ outage would cause a full database outage. Please enable multi_az = true.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "resource \"aws_rds_instance\" \"main\" {\n  identifier     = \"prod-db\"\n  instance_class = \"db.r5.large\"\n  multi_az       = false  # Single point of failure\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "resource \"aws_rds_instance\" \"main\" {\n  identifier     = \"prod-db\"\n  instance_class = \"db.r5.large\"\n  multi_az       = true  # Automatic failover\n}"
    }
  ],
  keyTakeaway: "Encode HA expectations in code. Single-AZ production deployments are unacceptable for critical services.",
  references: []
},

"terraform-hcl.reliability-observability-dr.are-single-points-of-failure-identified-and-either-removed-or-documented": {
  whatItMeans: "Single points of failure should be identified and either eliminated through redundancy or documented with explicit risk acceptance.",
  whyItMatters: "Unidentified single points of failure cause unexpected outages. Identified ones can be mitigated or accepted with a conscious risk decision.",
  howToVerify: "Review the architecture for single points of failure. Are there single NAT gateways, single-instance databases, or unreplicated services? Are they documented?",
  exampleComment: "There is only one NAT gateway for all private subnets. If it fails, all private subnet outbound traffic stops. Consider adding one per AZ.",
  codeExamples: [],
  keyTakeaway: "Identify every single point of failure. Eliminate or accept each one deliberately.",
  references: []
},

"terraform-hcl.reliability-observability-dr.are-autoscaling-boundaries-sensible-min-max-cooldowns-health-metrics": {
  whatItMeans: "Autoscaling configurations should have sensible min/max boundaries, appropriate cooldown periods, and health-based scaling metrics.",
  whyItMatters: "Autoscaling without limits can scale to zero (outage) or infinity (cost explosion). Cooldowns prevent thrashing. Health metrics ensure scaling responds to real demand.",
  howToVerify: "Check autoscaling configurations. Is min_capacity > 0 for production? Is max_capacity capped? Are cooldown periods set? Are scaling metrics appropriate?",
  exampleComment: "The min_capacity is 0 for production. If the service scales down completely, there will be an outage during cold start. Please set a minimum of at least 1.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "resource \"aws_appautoscaling_target\" \"ecs\" {\n  min_capacity = 0   # Can scale to zero = outage\n  max_capacity = 100 # No cost limit\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "resource \"aws_appautoscaling_target\" \"ecs\" {\n  min_capacity = 2   # Always running for HA\n  max_capacity = 20  # Cost-bounded\n}"
    }
  ],
  keyTakeaway: "Set sensible autoscaling boundaries. min > 0 for production, max capped for cost control.",
  references: []
},

"terraform-hcl.reliability-observability-dr.are-health-checks-and-readiness-liveness-settings-configured-appropriate": {
  whatItMeans: "Health checks for load balancers, container orchestrators, and autoscaling should be configured with appropriate paths, intervals, and thresholds.",
  whyItMatters: "Poor health check settings can cause healthy instances to be terminated (too aggressive) or unhealthy instances to continue receiving traffic (too lenient).",
  howToVerify: "Check health check configurations. Are paths hitting a meaningful endpoint? Are intervals and thresholds appropriate? Is the grace period sufficient for startup?",
  exampleComment: "The health check interval is 5 seconds with a threshold of 2. This is too aggressive and may kill instances during deployments. Consider 30s interval with threshold of 3.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "health_check {\n  path                = \"/\"\n  interval            = 5\n  healthy_threshold   = 2\n  unhealthy_threshold = 2\n  # Too aggressive - kills slow-starting instances\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "health_check {\n  path                = \"/health\"\n  interval            = 30\n  healthy_threshold   = 2\n  unhealthy_threshold = 3\n  timeout             = 10\n  matcher             = \"200\"\n}"
    }
  ],
  keyTakeaway: "Tune health checks for your application. Too aggressive kills healthy instances; too lenient serves bad traffic.",
  references: []
},

"terraform-hcl.reliability-observability-dr.are-backup-policies-present-frequency-retention-restore-testing-expectat": {
  whatItMeans: "Backup policies should define frequency, retention period, and restore testing expectations for all stateful resources.",
  whyItMatters: "Backups without defined policies may not exist when needed, may be too old to be useful, or may never have been tested for restorability.",
  howToVerify: "Check database and storage resources for backup configurations. Is automated backup enabled? Is retention appropriate? Is there a restore testing schedule?",
  exampleComment: "This RDS instance has backup_retention_period = 1 day. For production, please increase to at least 7 days.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "resource \"aws_rds_instance\" \"main\" {\n  identifier              = \"prod-db\"\n  backup_retention_period = 0  # No backups!\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "resource \"aws_rds_instance\" \"main\" {\n  identifier                = \"prod-db\"\n  backup_retention_period   = 14\n  backup_window             = \"03:00-04:00\"\n  copy_tags_to_snapshot     = true\n  delete_automated_backups  = false\n  final_snapshot_identifier = \"prod-db-final\"\n}"
    }
  ],
  keyTakeaway: "Backups must be defined, retained, and tested. Untested backups are just hopes.",
  references: []
},

"terraform-hcl.reliability-observability-dr.are-restore-runbook-assumptions-realistic-and-documented-rto-rpo": {
  whatItMeans: "Recovery time objective (RTO) and recovery point objective (RPO) should be documented and the infrastructure should be designed to meet them.",
  whyItMatters: "If the documented RTO is 1 hour but restoring a database takes 4 hours, the runbook is unrealistic. Infrastructure design must match recovery expectations.",
  howToVerify: "Check if RTO/RPO is documented. Does the backup frequency support the RPO? Does the restore process support the RTO? Has it been tested?",
  exampleComment: "The stated RPO is 1 hour but backups run daily. Please either increase backup frequency or adjust the RPO expectation.",
  codeExamples: [],
  keyTakeaway: "RTO and RPO must be realistic and tested. Paper promises do not survive real incidents.",
  references: []
},

"terraform-hcl.reliability-observability-dr.are-alarms-alerts-defined-for-critical-infra-saturation-and-failure-sign": {
  whatItMeans: "CloudWatch alarms or equivalent alerts should be defined for critical infrastructure metrics like CPU saturation, disk usage, error rates, and availability.",
  whyItMatters: "Without alerts, infrastructure failures are discovered by users rather than the operations team. Proactive alerting enables faster response and reduces impact.",
  howToVerify: "Check for CloudWatch alarms, Datadog monitors, or similar alerting on critical metrics. Are key saturation and failure signals covered?",
  exampleComment: "This RDS instance has no CloudWatch alarms. Please add alarms for CPU utilization, free storage space, and connection count.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "resource \"aws_rds_instance\" \"main\" {\n  identifier = \"prod-db\"\n  # No monitoring alarms defined\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "resource \"aws_cloudwatch_metric_alarm\" \"db_cpu\" {\n  alarm_name          = \"rds-prod-db-cpu-high\"\n  comparison_operator = \"GreaterThanThreshold\"\n  evaluation_periods  = 3\n  metric_name         = \"CPUUtilization\"\n  namespace           = \"AWS/RDS\"\n  period              = 300\n  statistic           = \"Average\"\n  threshold           = 80\n  alarm_actions       = [aws_sns_topic.alerts.arn]\n\n  dimensions = {\n    DBInstanceIdentifier = aws_rds_instance.main.id\n  }\n}"
    }
  ],
  keyTakeaway: "Define alerts for critical metrics. Users should not discover outages before the operations team.",
  references: []
},

"terraform-hcl.reliability-observability-dr.are-logs-metrics-traces-enabled-with-practical-retention-and-cost-contro": {
  whatItMeans: "Logging, metrics, and tracing should be enabled with retention periods that balance observability needs with cost control.",
  whyItMatters: "Too much logging without retention controls leads to runaway costs. Too little logging prevents debugging. The balance depends on the environment and compliance needs.",
  howToVerify: "Check CloudWatch log groups, S3 log buckets, and metric retention. Are retention periods set? Are costs projected for the logging volume?",
  exampleComment: "These CloudWatch log groups have no retention period set, defaulting to indefinite retention. Please set retention to 30 days for dev and 90 days for prod.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "resource \"aws_cloudwatch_log_group\" \"app\" {\n  name = \"/app/web\"\n  # No retention = keep forever = growing costs\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "resource \"aws_cloudwatch_log_group\" \"app\" {\n  name              = \"/app/web\"\n  retention_in_days = 90\n}"
    }
  ],
  keyTakeaway: "Set log retention policies. Unlimited retention is unlimited cost growth.",
  references: []
},

"terraform-hcl.reliability-observability-dr.are-dashboards-alerts-tied-to-ownership-and-escalation-paths": {
  whatItMeans: "Monitoring dashboards and alerts should be tied to specific team ownership with clear escalation paths.",
  whyItMatters: "An alert without an owner is an alert that nobody responds to. Clear ownership and escalation paths ensure timely incident response.",
  howToVerify: "Check alert configurations for SNS topics, PagerDuty integrations, or escalation policies. Is there a clear owner for each alert?",
  exampleComment: "These alarms send to a shared SNS topic with no clear owner. Please route them to the team-specific alert channel with an escalation policy.",
  codeExamples: [],
  keyTakeaway: "Every alert needs an owner. Unowned alerts are noise that erodes trust in monitoring.",
  references: []
},

"terraform-hcl.reliability-observability-dr.are-quota-and-api-rate-limits-considered-for-growth-scenarios": {
  whatItMeans: "Cloud service quotas and API rate limits should be considered when designing infrastructure for growth.",
  whyItMatters: "Hitting a service quota during a scaling event prevents autoscaling from working, causing an outage at the worst possible time.",
  howToVerify: "Review resources that scale (instances, containers, IPs). Are current quotas sufficient for expected growth? Are quota increase requests documented?",
  exampleComment: "The ECS service can scale to 50 tasks but the account's running task quota is 40. Please request a quota increase or lower the max.",
  codeExamples: [],
  keyTakeaway: "Quotas are hard limits. Plan for growth and request increases proactively.",
  references: []
},

"terraform-hcl.reliability-observability-dr.are-region-zone-failure-assumptions-reflected-in-architecture-choices": {
  whatItMeans: "Architecture choices should reflect assumptions about region and zone failure scenarios, with explicit decisions about what level of failure the system can tolerate.",
  whyItMatters: "A system designed for single-AZ resilience will not survive a regional outage. The architecture should match the required availability level.",
  howToVerify: "Review the architecture for failure assumptions. Is it multi-AZ? Multi-region? What is the expected behavior during each failure scenario?",
  exampleComment: "This is a single-region deployment but the SLA requires 99.99% availability. Consider adding multi-region failover or adjusting the SLA.",
  codeExamples: [],
  keyTakeaway: "Match architecture to availability requirements. Know what failures you can survive.",
  references: []
},

"terraform-hcl.reliability-observability-dr.are-maintenance-windows-patch-channels-represented-where-applicable": {
  whatItMeans: "Maintenance windows and patch channels for managed services should be configured to control when updates are applied.",
  whyItMatters: "Uncontrolled patching can cause unexpected downtime during business hours. Maintenance windows ensure updates happen during acceptable periods.",
  howToVerify: "Check managed services for maintenance window configurations. Are they set to off-peak hours? Are auto-minor-version upgrades configured intentionally?",
  exampleComment: "This RDS instance has no maintenance window set. It will use the default, which may not align with your availability requirements.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "resource \"aws_rds_instance\" \"main\" {\n  identifier = \"prod-db\"\n  # No maintenance window = provider default\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "resource \"aws_rds_instance\" \"main\" {\n  identifier           = \"prod-db\"\n  maintenance_window   = \"sun:03:00-sun:04:00\"\n  auto_minor_version_upgrade = true\n}"
    }
  ],
  keyTakeaway: "Configure maintenance windows explicitly. Do not leave patching timing to defaults.",
  references: []
},

"terraform-hcl.reliability-observability-dr.are-cost-impacting-changes-called-out-new-always-on-instances-egress-pre": {
  whatItMeans: "Changes that significantly impact cost (new always-on instances, NAT gateways, egress charges, premium tiers) should be highlighted in the PR.",
  whyItMatters: "Cost surprises are one of the most common complaints about cloud infrastructure. Calling out cost impacts during review prevents unexpected bills.",
  howToVerify: "Check for new resources with ongoing costs. Are cost implications noted in the PR? Has the estimated monthly cost been calculated?",
  exampleComment: "This PR adds 3 NAT gateways at approximately $100/month each plus data processing charges. Please note the cost impact in the PR description.",
  codeExamples: [],
  keyTakeaway: "Call out cost impacts during review. Surprise cloud bills damage trust.",
  references: []
},

"terraform-hcl.reliability-observability-dr.are-rightsizing-and-tier-choices-justified-for-environment-criticality": {
  whatItMeans: "Instance sizes and service tiers should be appropriate for the environment: production may need larger instances while dev can use smaller ones.",
  whyItMatters: "Over-provisioning wastes money. Under-provisioning causes performance issues. The right size depends on the environment and workload.",
  howToVerify: "Check instance types and service tiers. Are they appropriate for the environment? Is production using production-grade instances? Is dev using cost-effective options?",
  exampleComment: "This dev environment uses m5.4xlarge instances. Is this necessary for development? Consider t3.medium to reduce costs.",
  codeExamples: [],
  keyTakeaway: "Rightsize for the environment. Production needs reliability; dev needs affordability.",
  references: []
},

"terraform-hcl.reliability-observability-dr.are-lifecycle-retention-policies-used-to-control-storage-and-logging-cos": {
  whatItMeans: "Lifecycle and retention policies should be configured on storage and logging resources to automatically manage data growth and costs.",
  whyItMatters: "Without lifecycle policies, storage grows indefinitely. Old data accumulates, costs increase, and performance may degrade.",
  howToVerify: "Check S3 buckets, log groups, and other storage for lifecycle or retention policies. Are old objects transitioned to cheaper tiers or deleted?",
  exampleComment: "This S3 bucket has no lifecycle rules. Old data will accumulate indefinitely. Please add rules to transition to Glacier and expire old data.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "resource \"aws_s3_bucket\" \"logs\" {\n  bucket = \"app-logs\"\n  # No lifecycle rules - grows forever\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "resource \"aws_s3_bucket_lifecycle_configuration\" \"logs\" {\n  bucket = aws_s3_bucket.logs.id\n\n  rule {\n    id     = \"archive-and-expire\"\n    status = \"Enabled\"\n\n    transition {\n      days          = 30\n      storage_class = \"STANDARD_IA\"\n    }\n\n    transition {\n      days          = 90\n      storage_class = \"GLACIER\"\n    }\n\n    expiration {\n      days = 365\n    }\n  }\n}"
    }
  ],
  keyTakeaway: "Lifecycle policies are cost controls. Set them on every storage resource.",
  references: []
},

"terraform-hcl.reliability-observability-dr.are-idle-resources-prevented-in-ephemeral-environments-auto-stop-ttl-cle": {
  whatItMeans: "Ephemeral environments (dev, preview, test) should have auto-stop, TTL, or cleanup mechanisms to prevent idle resources from accumulating costs.",
  whyItMatters: "Forgotten ephemeral environments are a major source of cloud waste. Without cleanup mechanisms, they run indefinitely.",
  howToVerify: "Check ephemeral environment infrastructure for TTL or cleanup mechanisms. Is there a process for detecting and removing idle environments?",
  exampleComment: "These preview environments have no TTL or cleanup mechanism. Old preview environments will accumulate and waste money. Please add auto-deletion after 7 days.",
  codeExamples: [],
  keyTakeaway: "Ephemeral means temporary. Automate cleanup to prevent cost leaks from forgotten environments.",
  references: []
},

"terraform-hcl.reliability-observability-dr.are-tagging-standards-complete-for-cost-allocation-and-ownership-reporti": {
  whatItMeans: "All resources should be tagged with standard tags for cost allocation, ownership, environment, and project identification.",
  whyItMatters: "Without consistent tagging, cost allocation reports are useless. You cannot attribute costs to teams, projects, or environments, making cost optimization impossible.",
  howToVerify: "Check that all taggable resources have the required tags (Environment, Team, Project, CostCenter, ManagedBy). Are provider default_tags used?",
  exampleComment: "These resources are missing the CostCenter and Team tags required by our tagging policy. Please add them for accurate cost allocation.",
  codeExamples: [
    {
      label: "Bad",
      language: "hcl",
      code: "resource \"aws_instance\" \"web\" {\n  ami           = var.ami_id\n  instance_type = \"t3.micro\"\n  # No tags - invisible in cost reports\n}"
    },
    {
      label: "Good",
      language: "hcl",
      code: "resource \"aws_instance\" \"web\" {\n  ami           = var.ami_id\n  instance_type = \"t3.micro\"\n\n  tags = {\n    Name        = \"${local.name_prefix}-web\"\n    Environment = var.environment\n    Team        = var.team\n    CostCenter  = var.cost_center\n    Project     = var.project\n    ManagedBy   = \"terraform\"\n  }\n}"
    }
  ],
  keyTakeaway: "Tag everything. Untagged resources are invisible in cost and ownership reports.",
  references: []
},

"terraform-hcl.reliability-observability-dr.are-hidden-cost-multipliers-reviewed-cross-zone-region-traffic-nat-lb-da": {
  whatItMeans: "Hidden cost multipliers like cross-zone data transfer, NAT gateway data processing, load balancer data processing, and cross-region replication should be reviewed.",
  whyItMatters: "Data transfer charges are often the most surprising cloud costs. Cross-AZ traffic, NAT gateway processing, and LB data costs can dwarf compute costs.",
  howToVerify: "Review the architecture for data transfer paths. Is there cross-AZ traffic? NAT gateway usage? Load balancer data processing? Are these costs estimated?",
  exampleComment: "This architecture routes all traffic through a NAT gateway, which charges per GB processed. For high-traffic services, this could be very expensive. Consider VPC endpoints.",
  codeExamples: [],
  keyTakeaway: "Review data transfer paths. Hidden data processing charges are the most common cost surprise.",
  references: []
},

// ============================================================
// CI/CD, POLICY, TESTING & TEAM WORKFLOWS (items 173-195)
// ============================================================

"terraform-hcl.ci-cd-policy-testing-team-wo.are-plans-generated-in-ci-with-the-same-terraform-provider-versions-as-a": {
  whatItMeans: "CI plans and applies should use the same Terraform and provider versions to ensure the plan output accurately reflects what will be applied.",
  whyItMatters: "Version mismatches between plan and apply can cause different behavior, unexpected changes, or state corruption. The plan becomes untrustworthy.",
  howToVerify: "Check CI configuration. Is the Terraform version pinned? Are provider versions locked via .terraform.lock.hcl? Is the same version used for plan and apply?",
  exampleComment: "The CI plan step uses Terraform 1.6 but the apply step uses latest. Please pin both to the same version.",
  codeExamples: [],
  keyTakeaway: "Same versions for plan and apply. A plan with different versions is meaningless.",
  references: []
},

"terraform-hcl.ci-cd-policy-testing-team-wo.is-apply-gated-behind-review-approval-and-restricted-principals": {
  whatItMeans: "Terraform apply should only run after PR review and approval, and only by authorized principals (CI service accounts, not individual developers).",
  whyItMatters: "Ungated applies bypass the review process. Unrestricted principals mean anyone can apply changes, increasing the risk of unauthorized or unreviewed modifications.",
  howToVerify: "Check CI/CD pipeline configuration. Is apply triggered only after approval? Is it restricted to specific service accounts? Can developers trigger apply directly?",
  exampleComment: "The CI pipeline allows apply without approval. Please add a manual approval gate before apply for production environments.",
  codeExamples: [],
  keyTakeaway: "Gate apply behind review and approval. No one should apply unreviewed changes to production.",
  references: []
},

"terraform-hcl.ci-cd-policy-testing-team-wo.are-state-changing-commands-prevented-from-developer-laptops-for-protect": {
  whatItMeans: "State-changing commands (apply, destroy, state mv) should be prevented from running on developer laptops for protected environments like production.",
  whyItMatters: "Developer laptops lack the controls, audit logging, and version guarantees of CI environments. Running apply from a laptop risks using wrong versions, wrong state, or wrong credentials.",
  howToVerify: "Check backend and IAM configurations. Can developer IAM credentials apply to production? Are there guardrails preventing local applies to protected environments?",
  exampleComment: "Developers can run terraform apply against production from their laptops. Please restrict production apply to the CI service role only.",
  codeExamples: [],
  keyTakeaway: "Production applies should only happen through CI. Developer laptops are not trusted execution environments.",
  references: []
},

"terraform-hcl.ci-cd-policy-testing-team-wo.are-terraform-fmt-validate-and-lint-checks-enforced-in-ci": {
  whatItMeans: "terraform fmt, terraform validate, and linting tools (tflint, etc.) should be enforced in CI as pre-merge checks.",
  whyItMatters: "Automated checks catch formatting issues, validation errors, and common mistakes before they reach review, saving reviewer time and preventing bad code from merging.",
  howToVerify: "Check CI pipeline configuration. Are fmt, validate, and lint steps present? Do they run on every PR? Are they blocking?",
  exampleComment: "The CI pipeline does not run terraform validate. Please add it as a required check before merge.",
  codeExamples: [],
  keyTakeaway: "Automate the basics. fmt, validate, and lint should be non-negotiable CI checks.",
  references: []
},

"terraform-hcl.ci-cd-policy-testing-team-wo.are-policy-as-code-checks-enforced-opa-sentinel-conftest-tfsec-checkov-e": {
  whatItMeans: "Policy-as-code tools (OPA, Sentinel, Conftest, tfsec, Checkov) should be used to enforce security, compliance, and organizational policies automatically.",
  whyItMatters: "Manual policy checks are inconsistent and error-prone. Automated policy enforcement catches violations before they reach production.",
  howToVerify: "Check for policy-as-code tools in the CI pipeline. Are security policies (no public S3, encryption required) enforced? Are results visible in PR comments?",
  exampleComment: "There are no policy-as-code checks in the pipeline. Consider adding Checkov or tfsec to catch common security misconfigurations automatically.",
  codeExamples: [],
  keyTakeaway: "Automate policy enforcement. Manual security reviews do not scale.",
  references: ["https://www.checkov.io/", "https://github.com/aquasecurity/tfsec"]
},

"terraform-hcl.ci-cd-policy-testing-team-wo.are-high-risk-policy-violations-blocking-merges-applies": {
  whatItMeans: "High-risk policy violations (public databases, no encryption, wildcard IAM) should block merges and applies, not just warn.",
  whyItMatters: "Warnings are ignored. If a security violation is serious enough to flag, it should be serious enough to block. Non-blocking checks provide false security assurance.",
  howToVerify: "Check if policy-as-code findings are blocking or advisory. Are critical findings blocking the merge? Can they be overridden, and if so, is there an audit trail?",
  exampleComment: "The tfsec check flags a public S3 bucket as a warning but does not block the merge. Please make this a blocking check for high-severity findings.",
  codeExamples: [],
  keyTakeaway: "Critical policy violations should block, not warn. Warnings that are always ignored add no value.",
  references: []
},

"terraform-hcl.ci-cd-policy-testing-team-wo.are-plans-reviewed-for-both-resource-actions-and-attribute-level-drift": {
  whatItMeans: "Plan review should check not only resource-level actions (create, update, destroy) but also attribute-level changes that might indicate drift or unexpected behavior.",
  whyItMatters: "A plan showing 0 creates, 1 update can hide significant attribute changes within the update. Attribute-level review catches subtle but impactful changes.",
  howToVerify: "Review the detailed plan output, not just the summary. Are attribute-level changes expected? Do any changes suggest configuration drift?",
  exampleComment: "The plan shows 1 update but the detailed output reveals 12 attribute changes on the RDS instance. Please review each change carefully.",
  codeExamples: [],
  keyTakeaway: "Review plan details, not just summaries. The devil is in the attribute-level changes.",
  references: []
},

"terraform-hcl.ci-cd-policy-testing-team-wo.are-speculative-plans-generated-per-pr-for-all-impacted-stacks": {
  whatItMeans: "Every PR should generate speculative (non-applying) plan output for all stacks affected by the changes, so reviewers can see the full impact.",
  whyItMatters: "Without speculative plans, reviewers must guess the impact of code changes. A speculative plan shows the actual effect before merging.",
  howToVerify: "Check CI configuration. Is a speculative plan generated for every PR? Does it cover all affected stacks and environments?",
  exampleComment: "This PR changes a shared module but only generates a plan for dev. Please add speculative plans for staging and production to show the full impact.",
  codeExamples: [],
  keyTakeaway: "Generate speculative plans for every PR. Reviewers need to see what will actually change.",
  references: []
},

"terraform-hcl.ci-cd-policy-testing-team-wo.are-integration-smoke-tests-run-post-apply-where-feasible": {
  whatItMeans: "After terraform apply, integration or smoke tests should verify that the infrastructure is functional, not just that Terraform succeeded.",
  whyItMatters: "A successful apply does not guarantee working infrastructure. A database might be created but inaccessible. A load balancer might be configured but returning errors.",
  howToVerify: "Check for post-apply test steps in the pipeline. Do they verify connectivity, health endpoints, or basic functionality?",
  exampleComment: "There are no post-apply checks. Consider adding a smoke test that verifies the health endpoint responds after deployment.",
  codeExamples: [],
  keyTakeaway: "Apply success is not infrastructure success. Verify with post-apply tests.",
  references: []
},

"terraform-hcl.ci-cd-policy-testing-team-wo.are-module-contract-tests-present-inputs-outputs-expected-resources": {
  whatItMeans: "Reusable modules should have contract tests that verify inputs, outputs, and expected resource creation for different input combinations.",
  whyItMatters: "Contract tests catch regressions in module behavior before they affect consumers. They verify the module's interface and behavior match expectations.",
  howToVerify: "Check for terraform test files or terratest configurations. Do they test various input combinations? Do they verify outputs?",
  exampleComment: "This module has no tests. Please add terraform test files that verify the module creates expected resources with different input combinations.",
  codeExamples: [],
  keyTakeaway: "Module contract tests prevent regressions. Test inputs, outputs, and resource creation.",
  references: ["https://developer.hashicorp.com/terraform/language/tests"]
},

"terraform-hcl.ci-cd-policy-testing-team-wo.are-golden-plan-tests-or-snapshot-checks-used-judiciously-for-critical-m": {
  whatItMeans: "Golden plan tests (comparing plan output against a known-good snapshot) should be used for critical modules to detect unexpected changes.",
  whyItMatters: "Golden plan tests catch unexpected behavior changes that unit tests might miss. They verify the full plan output matches expectations.",
  howToVerify: "Check for plan snapshot tests. Are they maintained for critical modules? Are they updated intentionally when behavior changes?",
  exampleComment: "This critical networking module has no golden plan tests. Consider adding a snapshot test to catch unexpected plan changes.",
  codeExamples: [],
  keyTakeaway: "Golden plan tests are a safety net for critical modules. Use them judiciously.",
  references: []
},

"terraform-hcl.ci-cd-policy-testing-team-wo.are-drift-detection-jobs-scheduled-and-actionable": {
  whatItMeans: "Scheduled drift detection jobs should run terraform plan regularly to detect differences between code and actual infrastructure state.",
  whyItMatters: "Drift accumulates silently. Without scheduled detection, the team discovers drift only during the next apply, when it may cause unexpected changes or conflicts.",
  howToVerify: "Check for scheduled drift detection jobs. How often do they run? Are results monitored and acted upon? Is there a process for resolving detected drift?",
  exampleComment: "There is no drift detection configured. Consider scheduling a daily plan-only run that alerts the team when drift is detected.",
  codeExamples: [],
  keyTakeaway: "Schedule drift detection. Undetected drift is a ticking time bomb.",
  references: []
},

"terraform-hcl.ci-cd-policy-testing-team-wo.are-imports-migrations-rehearsed-in-lower-environments-before-prod": {
  whatItMeans: "State imports and migrations should be rehearsed in lower environments (dev, staging) before being applied to production.",
  whyItMatters: "State operations are high-risk. Rehearsing in lower environments catches issues before they affect production infrastructure.",
  howToVerify: "Check if the import or migration was tested in a lower environment. Are the results documented? Did the plan show the expected zero-change after import?",
  exampleComment: "This state import has not been tested in staging. Please rehearse the import in staging first and confirm zero changes in the subsequent plan.",
  codeExamples: [],
  keyTakeaway: "Rehearse state operations in lower environments. Production is not a testing ground.",
  references: []
},

"terraform-hcl.ci-cd-policy-testing-team-wo.are-branch-environment-promotion-flows-explicit-dev-stage-prod": {
  whatItMeans: "The flow for promoting changes through environments (dev to staging to production) should be explicit and enforced in the CI/CD pipeline.",
  whyItMatters: "Without explicit promotion flows, changes can skip environments and go directly to production without adequate testing.",
  howToVerify: "Check CI/CD pipeline configuration. Is there a clear promotion path? Can changes skip environments? Is each promotion gated by tests and approvals?",
  exampleComment: "This pipeline allows applying directly to production without going through staging. Please add a staging deployment step with approval before production.",
  codeExamples: [],
  keyTakeaway: "Enforce promotion flows. Changes should prove themselves in lower environments before reaching production.",
  references: []
},

"terraform-hcl.ci-cd-policy-testing-team-wo.are-environment-specific-backends-credentials-isolated-in-pipeline-confi": {
  whatItMeans: "Each environment should have isolated backend configurations and credentials in the CI pipeline to prevent cross-environment access.",
  whyItMatters: "Shared credentials across environments mean a dev pipeline compromise could affect production. Isolation limits blast radius.",
  howToVerify: "Check pipeline credentials and backend configurations. Are they separate per environment? Can the dev pipeline access production state?",
  exampleComment: "The same AWS credentials are used for dev and prod in the pipeline. Please create separate IAM roles per environment.",
  codeExamples: [],
  keyTakeaway: "Isolate credentials per environment. Shared credentials are shared risk.",
  references: []
},

"terraform-hcl.ci-cd-policy-testing-team-wo.are-manual-approval-steps-tied-to-risk-level-destroy-iam-networking-data": {
  whatItMeans: "Manual approval requirements should be proportional to the risk level: routine changes can be auto-approved, but destroys, IAM changes, and networking changes should require manual approval.",
  whyItMatters: "Requiring manual approval for every change causes review fatigue. Tiering approval requirements by risk level keeps high-risk changes safe without slowing down low-risk changes.",
  howToVerify: "Check CI/CD approval configuration. Are destroys, IAM, and networking changes gated by manual approval? Are low-risk changes auto-approved?",
  exampleComment: "This pipeline auto-applies all changes including destroys. Please add manual approval for plans that include destroy actions.",
  codeExamples: [],
  keyTakeaway: "Tier approvals by risk. High-risk changes need manual approval; routine changes should flow quickly.",
  references: []
},

"terraform-hcl.ci-cd-policy-testing-team-wo.are-emergency-change-paths-defined-with-auditability-and-follow-up-remed": {
  whatItMeans: "Emergency change procedures should be defined for situations requiring immediate infrastructure changes, with audit logging and follow-up remediation requirements.",
  whyItMatters: "Emergencies happen and sometimes bypass normal review processes. Having a defined emergency path ensures changes are still auditable and that follow-up review occurs.",
  howToVerify: "Is there a documented emergency change procedure? Does it require audit logging? Is there a follow-up review and remediation process?",
  exampleComment: "The team has no emergency change procedure. Please document one that includes audit logging, break-glass access, and mandatory follow-up review.",
  codeExamples: [],
  keyTakeaway: "Define emergency change procedures before you need them. Every emergency change needs follow-up review.",
  references: []
},

"terraform-hcl.ci-cd-policy-testing-team-wo.are-code-owners-reviewers-aligned-with-impacted-infra-domains": {
  whatItMeans: "CODEOWNERS and reviewer assignments should be aligned with the infrastructure domains affected by the change, ensuring the right experts review the right changes.",
  whyItMatters: "A networking change reviewed by the database team is not properly reviewed. Domain alignment ensures changes are reviewed by people who understand the implications.",
  howToVerify: "Check CODEOWNERS configuration. Are infrastructure domains mapped to the correct teams? Are reviewers auto-assigned based on files changed?",
  exampleComment: "This networking change was not auto-assigned to the networking team because CODEOWNERS is missing entries for the networking module. Please update CODEOWNERS.",
  codeExamples: [],
  keyTakeaway: "Align code owners with infrastructure domains. The right reviewer is the domain expert.",
  references: []
},

"terraform-hcl.ci-cd-policy-testing-team-wo.are-pr-templates-checklists-used-to-standardize-terraform-review-quality": {
  whatItMeans: "PR templates and checklists should be used to ensure consistent review quality for Terraform changes, prompting authors to include plan output, blast radius, and rollback plans.",
  whyItMatters: "Without templates, PRs vary in quality. Some include detailed plans and rollback strategies while others are bare diffs. Templates raise the floor for all PRs.",
  howToVerify: "Check for PR templates in .github/PULL_REQUEST_TEMPLATE.md. Do they prompt for plan output, environment target, blast radius, and rollback plan?",
  exampleComment: "There is no PR template for Terraform changes. Consider adding one that prompts for plan output, target environment, and rollback strategy.",
  codeExamples: [],
  keyTakeaway: "PR templates raise review quality consistently. They prompt for information that authors forget.",
  references: []
},

"terraform-hcl.ci-cd-policy-testing-team-wo.are-release-notes-changelogs-capturing-infra-impacting-behavior-changes": {
  whatItMeans: "Release notes and changelogs should capture infrastructure-impacting changes so that downstream consumers and operators are aware of behavioral changes.",
  whyItMatters: "Undocumented infrastructure changes surprise operators and consumers. Changelogs provide a historical record of what changed and why.",
  howToVerify: "Check for a CHANGELOG or release notes. Do they cover infrastructure-impacting changes? Are breaking changes highlighted?",
  exampleComment: "This module version bump includes a breaking change but has no changelog entry. Please document the change and migration steps.",
  codeExamples: [],
  keyTakeaway: "Document infrastructure changes in changelogs. Operators need to know what changed.",
  references: []
},

"terraform-hcl.ci-cd-policy-testing-team-wo.are-decommission-workflows-documented-drain-backup-revoke-destroy-verify": {
  whatItMeans: "Decommissioning infrastructure should follow a documented workflow: drain traffic, back up data, revoke access, destroy resources, and verify cleanup.",
  whyItMatters: "Ad hoc decommissioning risks data loss, orphaned resources, and security gaps from unrevoked access. A documented workflow ensures completeness.",
  howToVerify: "If resources are being decommissioned, is there a documented workflow? Are all steps (drain, backup, revoke, destroy, verify) addressed?",
  exampleComment: "This PR destroys the old database but does not mention backing up data or revoking service account access. Please follow the decommission workflow.",
  codeExamples: [],
  keyTakeaway: "Decommissioning is more than terraform destroy. Drain, backup, revoke, destroy, verify.",
  references: []
},

"terraform-hcl.ci-cd-policy-testing-team-wo.are-stale-modules-stacks-periodically-retired-to-reduce-attack-surface-a": {
  whatItMeans: "Unused modules and stacks should be periodically reviewed and retired to reduce the attack surface and maintenance burden.",
  whyItMatters: "Stale infrastructure accumulates security vulnerabilities, costs money, and confuses teams. Regular retirement keeps the codebase healthy.",
  howToVerify: "Check for modules or stacks that have not been modified or deployed recently. Are they still needed? Is there a process for periodic review and retirement?",
  exampleComment: "This module has not been updated in 18 months and is only used by one deprecated service. Consider retiring it as part of a cleanup sprint.",
  codeExamples: [],
  keyTakeaway: "Retire what you do not use. Stale infrastructure is a liability, not an asset.",
  references: []
},

"terraform-hcl.ci-cd-policy-testing-team-wo.are-post-incident-findings-translated-into-codified-terraform-guardrails": {
  whatItMeans: "After an infrastructure incident, the findings should be translated into codified guardrails: new policy-as-code rules, validations, or module defaults that prevent recurrence.",
  whyItMatters: "Incident findings that remain in a document are quickly forgotten. Codified guardrails permanently prevent the same class of issue from recurring.",
  howToVerify: "Check if recent incident findings have been translated into code. Are there new validation rules, policy checks, or module defaults that address past incidents?",
  exampleComment: "The post-incident review recommended requiring encryption on all databases, but this is not enforced in code. Please add a Checkov policy or module validation to codify this guardrail.",
  codeExamples: [],
  keyTakeaway: "Turn incident findings into code. Codified guardrails prevent recurrence better than documentation alone.",
  references: []
},

};  // end of module.exports
