import { ScrollView, Text, View, StyleSheet } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { colors, spacing, fontSizes, radius } from '../src/theme';
import { DesktopContainer } from '../src/components/DesktopContainer';

const SECTIONS = [
  {
    title: 'Purpose',
    body: 'ReviewHelm is designed to help you build better code review habits and improve your skills over time. Its checklists, guides, and AI-powered features are educational tools meant to support — not replace — your own judgment and expertise.',
  },
  {
    title: 'No Guarantee of Quality',
    body: 'Using ReviewHelm does not guarantee that your code, or code you review, is free of bugs, security vulnerabilities, performance issues, or other defects. Checklists are curated guidelines, not exhaustive specifications.',
  },
  {
    title: 'Not a Substitute for Human Review',
    body: 'ReviewHelm is intended to complement thorough human code review, not to replace it. Always apply your own critical thinking and domain knowledge when reviewing or shipping code.',
  },
  {
    title: 'AI-Generated Content',
    body: 'AI-powered features (deep dives, comment drafting, tutoring) are generated by large language models. While helpful, AI responses may contain inaccuracies, omissions, or outdated information. Always verify AI suggestions before acting on them.',
  },
  {
    title: 'Your Responsibility',
    body: 'You are solely responsible for the code you write, review, and approve. ReviewHelm is a learning aid — the final decision on code quality, security, and correctness is always yours and your team\'s.',
  },
];

export default function DisclaimerScreen() {
  return (
    <SafeAreaView style={styles.container} edges={['bottom']}>
      <DesktopContainer>
        <ScrollView contentContainerStyle={styles.content}>
          <Text style={styles.heading}>Disclaimer</Text>
          <Text style={styles.intro}>
            Please read this disclaimer carefully before using ReviewHelm.
          </Text>

          {SECTIONS.map((section) => (
            <View key={section.title} style={styles.card}>
              <Text style={styles.sectionTitle}>{section.title}</Text>
              <Text style={styles.sectionBody}>{section.body}</Text>
            </View>
          ))}

          <Text style={styles.lastUpdated}>Last updated: March 2026</Text>
        </ScrollView>
      </DesktopContainer>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: colors.bg,
  },
  content: {
    padding: spacing.lg,
    paddingBottom: spacing['4xl'],
  },
  heading: {
    fontSize: fontSizes['2xl'],
    fontWeight: '700',
    color: colors.textPrimary,
    marginBottom: spacing.sm,
  },
  intro: {
    fontSize: fontSizes.md,
    color: colors.textSecondary,
    marginBottom: spacing.xl,
    lineHeight: 22,
  },
  card: {
    backgroundColor: colors.bgCard,
    borderRadius: radius.md,
    padding: spacing.lg,
    marginBottom: spacing.md,
  },
  sectionTitle: {
    fontSize: fontSizes.md,
    fontWeight: '600',
    color: colors.textPrimary,
    marginBottom: spacing.sm,
  },
  sectionBody: {
    fontSize: fontSizes.sm,
    color: colors.textSecondary,
    lineHeight: 20,
  },
  lastUpdated: {
    fontSize: fontSizes.xs,
    color: colors.textMuted,
    textAlign: 'center',
    marginTop: spacing.lg,
  },
});
